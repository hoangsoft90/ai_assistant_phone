# handoff_20260923-2.md — Bàn giao phiên cho agent/người tiếp theo

> Ghi chú: skill `handoff` chỉ user gọi được (`disable-model-invocation: true`) — file này viết thủ
> công theo đúng mục đích của skill: phiên sau làm tiếp mà không cần đọc lại transcript.
> Chi tiết kết quả: `result_20260923-2.txt`. Nội dung không lặp lại các báo cáo đã có
> (`.plan/*-result.md`, `working.md`, `LESSONS_LEARNED.md`) — chỉ tham chiếu.

## 1. Bối cảnh 30 giây

App `ai_assistant_phone` (Flutter, Android-only, cá nhân): nghe hội thoại offline (mic → VAD → ASR
PhoWhisper/Vosk), gợi ý qua LLM (Groq), đọc gợi ý chỉ vào tai nghe A2DP. Đã đi qua P0→P7 + các phase
bổ sung. Buổi này: **review P5.1→P5.3** (chữa 5 test đỏ), **tối ưu N+1 HistoryScreen**, **đồng bộ tài
liệu**, **tách commit + push để CI build APK**.

## 2. Trạng thái hiện tại

- Test: **373/373 pass**, `flutter analyze` No issues found.
- Git: đã tách 4 commit phase + 1 commit docs, ĐÃ PUSH (`main`) — CI build APK đang chạy. Xem `git log -6`.
- App trên máy user: APK cũ (`4c699f6`) — **chưa có** P2.1/P5.1/P5.2/P5.3; cần APK mới từ CI rồi cài.
- Nợ verify máy thật gộp vào **K51** — giáo trình: `TESTING.md` + `human.md` (8 việc).

## 3. Bản đồ file quan trọng (chỉ những cái đổi gần đây)

| File | Vai trò |
|---|---|
| `lib/ui/root_scaffold.dart` | Khung 4 tab (P5.3); nhận `startService`/`stopService`/`capture` tuỳ chọn cho test |
| `lib/ui/session_coordinator.dart` | State + logic phiên dùng chung 4 tab; timer SnackBar 4.1s/snack ở `_drainSnackQueue` |
| `lib/ui/global_floating_controls.dart` | Nút nổi toàn cục (Bật/Dừng + Kết thúc buổi + Làm mới + Gợi ý) |
| `lib/ui/history_screen.dart` | Lịch sử phiên — đã tối ưu 2 query qua `sessionIdsWithReport()` |
| `lib/services/storage/transcript_dao.dart` | DAO; member mới `sessionIdsWithReport()` |
| `lib/ui/home_screen.dart` | Shim trỏ sang RootScaffold (giữ tên class cho tương thích) |
| `test/p5_3_navigation_test.dart` | 7 test nav — có `flushSnackTimers` + `_FakeCapture`; đọc trước khi thêm test UI |
| `TESTING.md` / `human.md` | Giáo trình test máy (human.md local-only, KHÔNG commit) |

## 4. Quyết định đã chốt trong phiên (kèm lý do)

1. **5 test P5.3 đỏ = hụt harness, không phải bug sản phẩm** — sửa bằng injection (tham số
   `capture` tuỳ chọn, đối xứng `startService`) chứ không đụng logic nghiệp vụ.
2. **Tối ưu N+1 bằng `sessionIdsWithReport()`** (1 query `SELECT DISTINCT session_id`) thay vì thêm
   method join phức tạp — bảng báo cáo nhỏ, `Set<int>` đủ dùng cho màn hình Lịch sử.
3. **Tách commit theo phase** (P2.1/P5.1/P5.2/P5.3 + docs) — user ra lệnh trực tiếp; TESTING.md +
   human.md để local-only.
4. **Asssertion test phải đối chiếu nhãn thật trong code**, không viết theo trí nhớ (lapse dẫn tới
   2/5 test đỏ ban đầu).

## 5. Việc tiếp theo — làm đúng thứ tự

1. Theo dõi CI run sau push; nếu fail → đọc log theo skill `ai-assistant-phone-debug-apk`.
2. APK xanh → nhắc user cài lên máy (đường dẫn artifact `app-debug-apk`).
3. Buổi test K51 theo `human.md` — sau buổi: tick DoD trong `.plan/*-result.md`, đóng nợ K, cập nhật
   `checklist.md`/`next.md`/`RELEASE_NOTES.md`.
4. Còn treo: signing release thật (chờ user); P6 Semi-auto (chờ dùng thực địa ≥ 2 tuần).
   Danh mục việc đầy đủ: `next.md` mục "Việc sắp tới".

## 6. Cần người dùng quyết (chưa chốt)

- Giữ `human.md`/`TESTING.md` local-only mãi không? (hiện ngoài git).
- Khi nào làm keystore release thật?

## 7. Bẫy cần nhớ khi tiếp tục (đã trả giá trong phiên này)

- Đọc A60/A61 trong `LESSONS_LEARNED.md`: (1) test widget bật phiên phải bơm fake capture — stub
  MethodChannel một mình KHÔNG đủ; (2) mọi test có SnackBar phải xả timer trước khi kết thúc.
- Sửa UI test: assert theo nhãn grep được trong `lib/ui/`, ListView phải scroll trước khi assert,
  nút nổi toàn cục luôn `findsOneWidget` ở mọi tab (không phải findsNothing).
- Thêm member vào `TranscriptDao` ⇒ phải cập nhật MỌI fake `implements TranscriptDao`
  (grep trước — hiện 5 file, weekly_stats dùng noSuchMethod nên thoáng).

## 8. Thứ không được làm

- Không đụng `floating_button.dart` (P5.3 khoá — 0 dòng được phép đổi).
- Không sửa nhánh migration `oldVersion < 2` / `< 3` (test khoá từng chữ).
- Không tự commit phần chạm audio/retention mà thiếu xác nhận user (vùng loại trừ Ponytail).
- Không báo "đã verify" cho phần chưa chạy trên máy thật — ma trận test luôn nêu rõ.

## 9. Suggested skills (dùng khi làm tiếp)

- `ai-assistant-phone-debug-apk` — đọc log CI/build APK khi run fail.
- `verification-before-completion` — trước mọi claim "xong/đã test".
- `flutter-device-smoke-test` — buổi test máy thật qua adb (K51).
