# handoff_20260923-3.md — Bàn giao phiên cho agent/người tiếp theo

> Viết thủ công theo đúng mục đích của skill `handoff` (skill đó chỉ user gọi được).
> Chi tiết kết quả: `result_20260923-3.txt`. Buổi trước: `handoff_20260923-2.md`.
> Nội dung không lặp báo cáo đã có (`.plan/*-result.md`, `working.md`, `LESSONS_LEARNED.md`) — chỉ tham chiếu.

## 1. Bối cảnh 30 giây

App `ai_assistant_phone` (Flutter, Android-only, cá nhân): nghe hội thoại offline (mic → VAD → ASR
PhoWhisper/Vosk), gợi ý qua LLM (Groq), đọc gợi ý chỉ vào tai nghe A2DP. Buổi này (2026-09-23 tối):
**bổ sung báo cáo `.plan/P5_3-result.md` còn thiếu**, **rà toàn bộ `.plan/`**, **đồng bộ tài liệu**
(`checklist.md`, `next.md`, `.project/openspec.md`, `bootstrap-shell.md`, `features.md`, `faq.md`),
**commit `9f678d2` + push**. 0 dòng code sản phẩm.

## 2. Trạng thái hiện tại

- Test: **373/373 pass** · `flutter analyze` sạch · `openspec validate --specs` **18/18**.
- Git: `main == origin/main` tại **`9f678d2`** (docs-only). CI: run cũ `35867829942` (`9259082`)
  **success** — artifact `app-debug-apk` 114 MB, chưa hết hạn; run mới `35879450819` (`9f678d2`)
  in_progress lúc bàn giao. **APK code-y hệt nhau** (commit docs-only) ⇒ cài được ngay cho K51.
- Báo cáo phase: **18/19 prompt có `*-result.md`**; chỉ thiếu `P6-result.md` — đúng thiết kế
  (P6 tuỳ chọn, chờ dùng thực địa ≥ 2 tuần).
- App trên máy user: vẫn APK cũ `4c699f6` ⇒ **phải cài APK mới trước buổi test K51**.

## 3. Bản đồ file quan trọng (đổi trong buổi này)

| File | Vai trò / thay đổi |
|---|---|
| `.plan/P5_3-result.md` | Báo cáo P5.3 mới bổ sung (gitignored — chỉ tồn tại trên máy) |
| `.project/openspec.md` | Bảng phase có đủ P2.1/P5.1/P5.2/P5.3; K50 gạch bỏ (thu hẹp ở P5.1); P7 🟡 phần tĩnh |
| `.project/modules/bootstrap-shell.md` | Viết lại theo P5.3: RootScaffold + 4 tab + nút nổi toàn cục; HomeScreen chỉ còn là shim |
| `next.md` / `checklist.md` | Đã phản ánh "commit+push xong"; mục cần hỏi lại còn treo (xem §6) |
| `features.md` / `faq.md` | Ngày cập nhật mới; P1F/P1G ghi bằng chứng máy 22/09; 2 mục FAQ lỗi thời đã sửa |
| `TESTING.md` / `human.md` | Giáo trình K51 — **local-only, KHÔNG commit** |

## 4. Quyết định đã chốt trong phiên (kèm lý do)

1. **`P6-result.md` không phải thiếu sót** — phase tuỳ chọn, cố ý bỏ; tài liệu nhiều nơi ghi đúng.
2. **Số liệu trong báo cáo phải chạy lại tươi** — P5_3-result.md viết bằng kết quả analyze/test chạy
   tại thời điểm viết, không chép "372" từ vòng cũ (thực tế 373).
3. **`.plan/` khi cần chia sẻ → chép sang `.project/`** (commit được), không đề nghị "copy sang docs/"
   nữa (FAQ cũ đã sửa).
4. **Commit docs được tự động sau khi user ra lệnh; TESTING.md/human.md luôn local-only.**

## 5. Việc tiếp theo — làm đúng thứ tự

1. Kiểm run `35879450819` xanh (hoặc dùng artifact run `35867829942` — code như nhau).
2. Hướng dẫn user tải artifact `app-debug-apk` → cài máy (thay APK `4c699f6` cũ).
3. Buổi test **K51** theo `human.md` (8 việc) + `TESTING.md` — gộp mọi nợ máy thật:
   nav 4 tab, migration DB có sẵn v2→v4, đổi tên phiên, retention, LLM endpoint thật,
   Pre-Brief/summary/Post-Review/Level, half-duplex K45, P1F TC1/TC3.
4. Sau buổi test: tick DoD trong `.plan/*-result.md`, đóng nợ K (K34/K36/K37/K39/K41/K42/K45/K46/
   K47/K48/K49/K51), cập nhật `checklist.md`/`next.md`/`RELEASE_NOTES.md`.
5. Còn treo: signing release thật (chờ user); P6 Semi-auto (chờ ≥ 2 tuần thực địa).

## 6. Cần người dùng quyết (chưa chốt)

- Giữ `human.md`/`TESTING.md` local-only mãi không? (hiện ngoài git theo `.gitignore` custom).
- `.project/openspec.md` §4 "Todo ngay tiếp theo" chưa liệt kê K51 + P2.1/P5.1–P5.3 — thêm không?
- Khi nào làm keystore release thật?

## 7. Bẫy cần nhớ khi tiếp tục (đã trả giá trong phiên này)

- **Hoàn thành = code + artifact bàn giao.** P5.3 từng "xong code, mất báo cáo" — khi kết thúc phase,
  kiểm `.plan/<PHASE>-result.md` tồn tại trước khi báo xong.
- Số liệu (số test, số dòng, size) phải lấy từ lệnh chạy **tại thời điểm viết**; số cũ trong
  working.md/báo cáo cũ có thể lệch 1 vòng chạy.
- Chèn hàng vào bảng markdown ⇒ đếm `|` của các hàng lân cận ngay sau khi sửa.
- Mục `[x]` phải nằm đúng khu "Đã làm" — sau khi chuyển mục, đọc lại 10 dòng quanh nó.
- Bài học kỹ thuật cũ vẫn hiệu lực: A60 (fake capture cho test widget), A61 (xả timer SnackBar),
  A24 (thêm member DAO ⇒ cập nhật mọi fake `implements`).

## 8. Thứ không được làm

- Không đụng `floating_button.dart` (P5.3 khoá — 0 dòng được phép đổi).
- Không sửa nhánh migration `oldVersion < 2` / `< 3` (test khoá từng chữ).
- Không tự commit phần chạm audio/retention/DB mà thiếu xác nhận user (vùng loại trừ Ponytail).
- Không báo "đã verify" cho phần chưa chạy trên máy thật — ma trận test luôn nêu rõ.
- `.project/ai-rules.md` KHÔNG tồn tại — đừng mất thời gian tìm; quy ước knowledge base nằm ở
  `.project/README.md` + `architecture.md` (thứ tự đọc 6 bước).

## 9. Suggested skills (dùng khi làm tiếp)

- `ai-assistant-phone-debug-apk` — đọc log CI/build APK khi run fail.
- `verification-before-completion` — trước mọi claim "xong/đã test".
- `flutter-device-smoke-test` — buổi test máy thật qua adb (K51).
