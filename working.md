# working.md — nhật ký đang làm

- [2026-09-24] **Fix SnackBar không hiện — `ScaffoldMessenger` đặt SAI vị trí (code + 4 test mới, 428/428 pass, analyze sạch; CHƯA commit, chưa test máy).** Làm theo `.plan/prompt_fix_snackbar_messenger.md`. **Nguyên nhân (đã xác minh bằng chính Flutter SDK, `material/scaffold.dart` dòng 211–245):** `ScaffoldMessenger(key: _messengerKey)` nằm **trong** `Scaffold.body` ⇒ ở DƯỚI `Scaffold`, ngược thứ tự đúng. `ScaffoldMessengerState._updateScaffolds` chỉ vẽ SnackBar cho `Scaffold` thoả `_isRoot` (không có tổ tiên `Scaffold` nào đã đăng ký) ⇒ messenger của key chỉ thấy `Scaffold` của `HistoryScreen`/`StatsScreen` — mà hai màn đó nằm **offstage trong `IndexedStack`** ⇒ bấm ở tab **Trang chủ/Cài đặt** thì SnackBar bị vẽ trong cây tab: **vô hình**. Đúng triệu chứng user báo (Test LLM im lặng). **Fix:** đảo vị trí 2 widget (`ScaffoldMessenger` ra ngoài, `Scaffold` vào trong) — `git diff` xác nhận `appBar`/`IndexedStack`/`BottomNavigationBar` **không đổi nội dung nào**; **KHÔNG** đổi `enqueueSnack`/`_drainSnackQueue` (đúng constraint). `HistoryScreen`/`StatsScreen` **giữ nguyên** `Scaffold` riêng (Flutter tự xử qua `_isRoot` ⇒ đúng 1 SnackBar, không vẽ trùng). Bonus: 4 chỗ `ScaffoldMessenger.of()` trong `history_screen.dart` + nút Test LLM **trong dialog cấu hình LLM** cũng được sửa theo. **Test mới** `test/snackbar_visibility_test.dart` (4): Test LLM OK/fail + Bật lắng nghe từ tab Trang chủ (tab KHÔNG có `Scaffold` riêng — ca quan trọng nhất) + từ tab Lịch sử; bất biến khoá đúng lỗi cũ: SnackBar phải **không** có tổ tiên `IndexedStack`. **Đã kiểm test biết đỏ:** tạm đảo cây về bản cũ ⇒ **4/4 đỏ** (`Found 0 widgets with type "SnackBar"` / `Found 1 widget with type "IndexedStack" that are ancestors of...`) ⇒ khôi phục ⇒ 4/4 xanh. **Thêm seam test** `TestLlmService? testLlm` (tuỳ chọn, mặc định `null`) vào `RootScaffold`+`SessionCoordinator`. **Sửa 1 test CŨ đỏ vì time-bomb (không phải do fix):** `p5_4_catchup_test.dart` "DoD 5 — throttle 6 giờ" — fake DAO dùng `DateTime.now()` **thật** còn service được bơm `now:` cố định ⇒ chỉ xanh trước 03:00 ngày 24/09/2026; đã chứng minh bằng `git stash` (vẫn đỏ y hệt khi bỏ 2 file tôi sửa); sửa tất định bằng hằng `_fixedNow` dùng chung. **Bài học mới A72.** DoD **còn thiếu 1 mục:** test tay trên máy thật (không có Android SDK/thiết bị ⇒ checklist `.plan/manual_test_snackbar_messenger.md`). **OCR không khả dụng** (`ocr` MISSING) + **AgentMemory UNREACHABLE** ⇒ fallback review mặc định (đã làm, có ghi rõ trong báo cáo). Báo cáo: `.plan/fix_snackbar_messenger-result.md` + `result_20260924-2.txt` + `handoff_20260924-2.md`.

- [2026-09-24] **P5.4 — Timeout theo use-case + Phân tích bù phiên thiếu báo cáo (code + 25 test mới, 424/424 pass, analyze sạch; CHƯA commit, chưa verify máy).** Làm theo `.plan/prompt_P5_4.md`; báo cáo `.plan/P5_4-result.md`. **A:** `SuggestionConfig.postReviewTimeout` = 5 phút cho Post-Review + Session Summary; `llmTimeout` 4s của Push GIỮ NGUYÊN (không đổi); `TestLlmService` 12s→30s; thêm `llm_http_client.dart` (`IOClient` + `connectionTimeout` 10s) dùng chung cho provider + Test LLM. **B:** schema **v6** (cột `last_analysis_attempt_ms`, chỉ thêm nhánh `< 6`); `TranscriptDao` +3 method (`finishedSessionsWithoutReport`/`markAnalysisAttempted`/`fullSessionText`); logic ghép-cắt transcript gom về `joinSessionText` (một bản duy nhất, `TranscriptStore` uỷ quyền) ⇒ "phân tích ngay" và "phân tích lại sau" không thể lệch; `PostReviewService.runForSession` + `_analyze` dùng chung + cờ `infrastructureFailure`; `pending_analysis_service.dart` mới (tuần tự, throttle 6h, dừng lượt khi lỗi hạ tầng); 2 trigger: `SessionCoordinator.init()` (unawaited + cờ chống chồng) và nút AppBar trong `HistoryScreen`. **Sửa 1 test cũ:** p5_2 ghim cứng `databaseVersion == 5` (đã vỡ lần 2) → assert theo quy tắc `== max(oldVersion < N)`. **Bài học mới A67–A71** (null-promotion sau try/catch; `grep -c noSuchMethod` theo file ≠ theo class; test ghim số; `pumpAndSettle` + animation vô hạn; ngoặc lồng trong `unawaited`) + skill `.agents/skills/flutter-dart-agent-pitfalls/`.

- [2026-09-23] **Đồng bộ `.project/` + đã commit/push (3 commit tới `d4f4943`, CI đang build APK).** Lô gồm: `370ac0f` issue1_fix (v5, Test LLM, lifecycle) · `efdf25e` follow-up summary custom-LLM · `d4f4943` docs. `.project/openspec.md`: +2 hàng phase (issue1_fix, follow-up), K51 bổ sung 4 mục máy-thật mới; `.project/modules/storage.md` viết lại theo schema v5 (trước lệch nặng: ghi v1/không có migration); coaching.md + suggestion-engine.md cập nhật. TESTING.md + human.md giữ local-only. K51 = buổi test máy thật gộp tiếp theo (cần APK từ run `d4f4943`).
- [2026-09-23] **Follow-up issue1_fix: SessionSummaryService theo custom LLM (code + 3 test mới, 399/399 pass, analyze sạch; chưa commit).** Tham số `ConfigStore? llmConfigStore` vào `SessionSummaryService` (cùng pattern PostReview/Suggestion/TestLlm) ⇒ tóm tắt phiên đi đúng endpoint/model tuỳ chỉnh khi không inject provider; chuỗi khởi tạo trong app đã OK từ P2.1. Prompt tóm tắt + nhịp refresh + session lifecycle KHÔNG đổi. Test mới dùng mock HTTP server cục bộ (127.0.0.1) + stub kênh secure-storage + HttpOverrides (A66). Spec: `suggestion-engine` (+Requirement P2.1 resolve-mỗi-lần-gọi) + `coaching` (nguồn cấu hình tóm tắt) — validate 18/18. Docs đồng bộ: checklist/next(13f)/faq(+2)/features/result_20260923-4/handoff_20260923-4/LESSONS(A65,A66).

- [2026-09-23] **issue1_fix — Fix LLM Config + Session Lifecycle (code + 23 test mới, 396/396 pass, analyze sạch; chưa commit, chưa verify máy).** Làm theo `.plan/issue1_fix.md`. **LLM:** message thiếu key GENERIC (custom endpoint không còn "chưa có API key Groq" sai ngữ cảnh — test khoá cả 2 chiều); `TestLlmService` mới (request thật `Reply with exactly: OK`, max_tokens 8, timeout 12s, phân loại auth/notFound/invalidEndpoint/timeout/network/server, dùng CÙNG resolver+SecureStore với Post-Review, KHÔNG đụng session — test `reads==0`); dialog Settings điền sẵn giá trị PERSISTED + key PLAINTEXT (debug-only theo mục 13, comment mốc P7, không log key); nút Test LLM ở tab Cài đặt + trong dialog (dùng giá trị đang hiện trên UI, loading state, chặn spam). **Session lifecycle:** schema **v5** (cột `ended_at_ms`, nhánh `oldVersion < 5`, nhánh cũ giữ nguyên từng chữ); `TranscriptSession.endedAt/isFinished` + `DAO.markSessionEnded`; `TranscriptStore._open` chỉ resume khi **chưa finished** + trong resumeGap; `finishSessionAndReview` capture sessionId TRƯỚC mọi async → stop → markEnded → Post-Review. **2 test cũ cập nhật có lý do:** p5_2 (version 4→`>=4`+`==5`), p5_3 (label "(Groq)"→generic + assert nút Test LLM). Fakes thêm `markSessionEnded` (A24). **Remaining:** K-máy-thật (migration v4→v5, endpoint thật); `SessionSummaryService` vẫn không theo custom endpoint (ngoài scope — ghi lại chưa sửa). Báo cáo: `.plan/issue1_fix-result.md`.

- [2026-09-23] **Buổi tối: đóng gói tài liệu phiên + đồng bộ toàn bộ knowledge base (docs-only, sau commit `9f678d2`).** User yêu cầu update openspec/checklist/features/next/faq/result/handoff/LESSONS_LEARNED "nếu chưa" — rà và chỉ sửa cái thật sự lệch: (1) **`.project/modules/bootstrap-shell.md` viết lại theo P5.3** (RootScaffold + 4 tab + nút nổi toàn cục; `main.dart` `home: RootScaffold()`; HomeScreen chỉ còn shim — bản cũ mô tả HomeScreen ScreenSet không còn đúng); (2) `features.md` đổi ngày + ghi rõ bằng chứng máy 22/09 của P1F/P1G; (3) `faq.md` sửa 2 mục lỗi thời ("repo 0 commit" → `9f678d2`; "`.plan/` ngoài git" → quy ước chép sang `.project/` khi chia sẻ); (4) **`result_20260923-3.txt`** + **`handoff_20260923-3.md`** (kế thừa format buổi chiều; ghi rõ `.project/ai-rules.md` KHÔNG tồn tại — user nhắc nhầm tên, quy ước thật là `.project/README.md`); (5) **`LESSONS_LEARNED.md` +A62/A63/A64** (sót deliverable bàn giao; số liệu phải chạy tươi; kiểm cấu trúc bảng/khu vực sau khi chèn). CI: run cũ `35867829942` success — **APK cài được ngay** cho K51; run docs-only `35879450819` không đổi APK. **Phần thay đổi docs này CHƯA commit** — chờ lệnh.

- [2026-09-23] **Đồng bộ `.project/openspec.md` (docs-only, user ra lệnh).** Sửa **3 chỗ** (2 theo yêu cầu + 1 phát sinh cùng loại): (a) **thêm 4 hàng `P2.1`/`P5.1`/`P5.2`/`P5.3`** vào bảng phase (trước đó bảng dừng ở P5 → P6, thiếu hẳn nhóm bổ sung) kèm schema v3/v4 + số test 320/343/365/373; (b) **`K50` đánh dấu ~~đã thu hẹp~~** — báo cáo Post-Review đã persist ở P5.1 (bảng `post_review_reports`), nợ còn lại chỉ là xem lại từ Lịch sử trên máy thật (gộp K51); (c) **hàng `P7` từ `⬜` → `🟡 phần tĩnh xong`** (EthicsGate + R8/minify build xanh, APK release 70.9 MB) cho khớp `next.md` và chính K51 trong file. Đổi header "sau P5" → "sau P5.3". Đã kiểm số cột: mọi hàng bảng phase = 4 dấu `|` (3 cột), hàng K = 5 dấu `|` (4 cột) — không vỡ bảng. **Còn nợ nhỏ chưa sửa (chờ lệnh):** mục "4. Todo ngay tiếp theo" liệt kê phase cần test máy thật nhưng **chưa có K51/P2.1/P5.1–P5.3**.

- [2026-09-23] **Rà `.plan/` + đồng bộ `checklist.md`/`next.md` (docs-only).** Kết quả rà: 19 `prompt_*.md`, 18 có `*-result.md` — **chỉ thiếu `P6-result.md`, đúng thiết kế** (P6 semi-auto là phase tuỳ chọn, precondition "dùng thực địa ≥ 2 tuần", user chốt bỏ để sang P7; `features.md`/`next.md`/`.project/openspec.md` đều đã ghi "chưa làm"). Đã sửa 2 file user yêu cầu: `next.md` (bảng 13e 372→**373**, thêm đường dẫn `P5_3-result.md`, mục "Việc sắp tới #0" từ todo → ✅ xong với 5 commit tới `9259082`); `checklist.md` (thêm mục báo cáo P5.3 + xác nhận push). **Phát hiện drift thứ 3 CHƯA sửa (ngoài phạm vi yêu cầu) — `.project/openspec.md`:** (a) bảng phase dừng ở P0.5→P7, **thiếu hàng 13b–13e** (P2.1/P5.1/P5.2/P5.3) dù `next.md` có; (b) **K50 "báo cáo Post-Review không persist" đã lỗi thời** — P5.1 đã persist (bảng `post_review_reports`). Đã ghi cả 2 vào `checklist.md` mục "cần hỏi lại", chờ lệnh. **CI cho `9259082`: run `35867829942` → success, artifact `app-debug-apk` 114 MB (chưa hết hạn)** ⇒ APK mới đã sẵn sàng cho vòng test máy thật K51.

- [2026-09-23] **Bổ sung báo cáo còn thiếu `.plan/P5_3-result.md` (P5.3 đã code + commit + push trước đó, chỉ hụt file kết quả).** Viết lại theo đúng format `P5_2-result.md`, mọi số liệu chạy lại tươi: `flutter analyze` No issues found · `flutter test` **373/373** · `p5_3_navigation_test.dart` 7 test · `git diff 4c699f6..HEAD --name-only | grep -c floating_button` = **0** (ràng buộc prompt giữ nguyên). Đối chiếu thêm: nhãn `_infoRow` trước/sau khi tách — bản cũ (`4c699f6`) **15 dòng** ⇒ `home_tab` mới giữ đủ **15** (4 card rút gọn + 11 trong ExpansionTile "Chi tiết kỹ thuật"), 0 dòng mất; prompt ghi "13" là con số ước lượng. `.plan/` gitignored ⇒ không cần commit.

- [2026-09-23] **Review P5.1→P5.3 (OCR không có ⇒ fallback review mặc định) — bắt + sửa hụt test P5.3, 372/372 pass · analyze sạch.** Chạy `analyze` + full test trước khi review: **5 test P5.3 đỏ** — soi từng lỗi ra 3 nguyên nhân, **không cái nào là bug sản phẩm**: (1) harness thiếu — test P5.3 chỉ stub MethodChannel nhưng `NativeCaptureClient.startNative` nhận `null` ⇒ `FormatException` ⇒ CaptureUnavailable ⇒ phiên không bao giờ bật trong test; sửa bằng bơm **fake capture** (cùng mẫu `_FakeCapture` của P4) qua tham số `capture` MỚI của `SessionCoordinator`/`RootScaffold` (đối xứng `startService` đã có, default null = code cũ từng byte); (2) assertion viết theo trí nhớ bản cũ — `'chưa ghi'` không tồn tại trong `home_tab` (nhãn thật `'Quyền'`…) và đòi nút nổi `findsNothing` ở tab khác — MÂU THUẪN với chính DoD 2 (nút nổi phải hiện ở MỌI tab); (3) test kết thúc khi timer SnackBar (4s) + hàng đợi snack coordinator (4.1s/snack, `_drainSnackQueue`) còn pending ⇒ invariant `!timersPending` fail — thêm `flushSnackTimers` (bơm 10s đồng hồ giả); (4) tab Cài đặt là ListView build lười ⇒ phải `scrollUntilVisible` trước khi assert. Kèm 2 lỗi tự mắc trong lúc sửa (import thiếu `MethodChannel`; assert sai 'Kết thúc buổi' ở Cài đặt — phải `findsOneWidget` vì đó là nhãn nút nổi) — đều tự bắt qua chạy test. Kiểm ràng buộc prompt: `git diff --name-only | grep floating_button` = **0 dòng** (file không bị đụng), shim `home_screen.dart` giữ tương thích, secret scan sạch.
- [2026-09-23] **P5.2 — Đặt tên phiên (mặc định theo timestamp, sửa được sau) — code + 22 test mới xong, 365/365 pass, analyze sạch; chưa verify máy.** Làm theo `.plan/prompt_P5_2.md`. **Precondition đọc version THẬT** (`grep` ⇒ `databaseVersion = 3`) ⇒ lên **4**, nhánh mới `oldVersion < 4` (không hard-code). **Schema:** cột `title TEXT` (NULL hợp lệ) thêm bằng helper `_addSessionTitleColumn()` gọi từ `onCreate` + nhánh `< 4` — **cố ý KHÔNG nhét vào `_createTranscriptSchema`** vì helper đó còn được nhánh `< 2` gọi ⇒ sẽ `duplicate column name` khi nâng từ DB v1 (đã chứng minh bằng sqlite3 thật). Nhánh `< 2`/`< 3` giữ nguyên từng chữ. **Tên mặc định:** không ghi gì lúc "Kết thúc buổi" (để NULL), sinh lúc hiển thị qua `SessionDisplayName.of(session)` (`lib/transcript/session_display_name.dart`) — dùng ở danh sách Lịch sử + tiêu đề màn hình chi tiết; kèm `timestamp()`, `defaultFrom()`, `normalize()` (rỗng/toàn khoảng trắng ⇒ NULL, >60 ⇒ cắt, không ném). **Đổi tên:** nút bút chì mỗi dòng → dialog điền sẵn tên đang hiển thị (`TextFormField.initialValue`, không dùng controller ⇒ không phải dispose) → `TranscriptDao.renameSession(sessionId, String?)` → cập nhật NGAY tại chỗ. **Chuẩn hoá ở tầng UI** (giới hạn 60 ký tự là yêu cầu layout; tránh vòng import services/storage ↔ transcript). **Bằng chứng mạnh:** trích NGUYÊN VĂN SQL từ `app_database.dart` rồi chạy trên `sqlite3` 3.45.1 — v1→v4 OK (có cột title, meta nguyên vẹn), v3→v4 với dữ liệu thật **1/1/1/1 → 1/1/1/1** (không mất gì, title=NULL cho phiên cũ), đổi/xoá tên OK, và chứng minh nếu nhét title vào CREATE TABLE thì sqlite3 báo `duplicate column name: title`. Test mới 22 (tên hiển thị, chuẩn hoá, migration đọc nguồn, 7 test widget). Sửa 1 assertion test P5.1 (`databaseVersion` 3 → `greaterThanOrEqualTo(3)`) vì P5.2 buộc tăng lên 4. Hồ sơ: `.project/modules/transcript-store.md` + `openspec/specs/transcript-store/spec.md` (18/18 validate). Báo cáo: `.plan/P5_2-result.md`. **Chưa commit.**

- [2026-09-23] **P5.1 — Lịch sử phiên + Lưu báo cáo Post-Review + Retention chỉnh được — code + 23 test mới xong, 343/343 pass, analyze sạch; chưa verify máy.** Làm theo `.plan/prompt_P5_1.md`; Precondition đạt (P5 xong + đọc đúng 4 file chỉ định). **Schema v3:** bảng `post_review_reports` (8 cột + index) — nhánh `oldVersion < 3` thêm MỚI, nhánh `< 2` giữ nguyên từng chữ (khoá bằng test đọc source thật); migration chỉ CREATE, không đụng dữ liệu cũ. **Lưu báo cáo trong `PostReviewService._persist`** (đúng ưu tiên prompt): chỉ khi `isUsable`, `sessionId` lấy TRƯỚC khi caller đổi phiên, bọc try/catch — DB lỗi ⇒ log, vẫn trả báo cáo cho UI; tách `ReportSink` interface + `SqliteReportSink` (test không cần SQLite). **HistoryScreen** (pattern `StatsScreen`): phiên có báo cáo ⇒ mở `ReportDetailScreen` đúng 3 mục; không có ⇒ SnackBar "Phiên này chưa có báo cáo.", không mở trống; DB lỗi ⇒ hiện lỗi không crash; widget `ReportSections` dùng chung giữa Post-Review mới và xem lại (tránh trùng code). **Retention:** khoá `storage.retention_days` (meta), resolver không-ném (chưa cấu hình/hỏng/≤0 ⇒ 7 ngày — hành vi y hệt trước phase, 320 test cũ không sửa vẫn pass), `TranscriptStore.instance()` tạo với `MetaConfigStore` + `_open()` đọc hạn qua resolver (constructor giữ `retention` cứng cho test); dropdown Settings 3/7/14/30 (không nhập tự do), **đổi hạn ⇒ `RetentionCleanup.runNow` xoá NGAY** — cleanup phủ CẢ `post_review_reports` trong cùng transaction `deleteOlderThan` (báo cáo không sống lâu hơn transcript). **Quyết định prompt giao chọn:** dùng `allSessions({limit})` thay vì tái dùng `sessionsSince(mốc rất xa)` (mốc cứng hỏng với phiên trước epoch; tên hàm đúng ý). OCR không khả dụng ⇒ fallback review mặc định: soi diff 6 file lib/, secret scan sạch, dead-code check sạch; 1 helper vô nghĩa viết nháp trong `HistoryScreen._load` đã tự phát hiện + thu hồi. Báo cáo: `.plan/P5_1-result.md`. **Chưa commit.**
- [2026-09-23] **Review P5.1 trước commit (bắt 3 lỗi thật — đã sửa, 343/343 vẫn xanh + analyze sạch).** (1) `app_database.dart`: dòng `);  await db.execute(` gộp 2 câu lệnh + block `_createTranscriptSchema` bị thụt lề sai — khôi phục đúng dạng. (2) 2 doc comment **lạc chỗ**: comment của `instance()` bị nhân đôi nằm trên `_instance`; comment của `SqliteReportSink` nằm giữa file (trên `PostReviewReport`) — trả về đúng chỗ. (3) `ReportSink.save(int sessionId, row)` **thừa tham số** (`row` đã mang `sessionId`, có nguy cơ truyền lệch phiên) ⇒ gọn còn `save(row)` (sửa 2 impl + 1 fake test). **Giữ nguyên (có lý do):** `HistoryScreen._load` chạy N+1 query (`reportForSession` cho từng phiên) — chấp nhận vì `allSessions` đã chặn `limit: 100` (≤100 truy vấn chỉ mục, sqflite chạy nền), đổi thành 1 query phải chạm interface + 5 impl/fake — không đáng trước commit.

- [2026-09-23] **P2.1 — Custom LLM Provider (endpoint/model tuỳ chỉnh) — code + 11 test mới xong, 320/320 pass, analyze sạch; chưa verify máy.** Làm theo `.plan/prompt_P2_1.md`; Precondition đạt (P2 xong + đọc đúng 3 file chỉ định). **2 quyết định agent được phép chọn:** (1) mục 2 không chọn nguyên văn (a)/(b) mà thêm `ConfigStore? configStore` tuỳ chọn vào `GroqLlmProvider` — provider tự đọc endpoint/model **mỗi lần gọi** (đối xứng cách key đã đọc; không cache ⇒ cấu hình hiệu lực ngay, không cần restart) vì chuỗi khởi tạo thật toàn sync `late final` 5 lớp, cả (a)/(b) đều phải async hoá 3 service — rủi ro regression cao hơn giá trị; (2) mục 3 chọn **(a) URL đầy đủ** (khớp field `endpoint` hiện có, UI đặt "Endpoint URL"). 2 khoá `llm.base_url`/`llm.model` ở bảng `meta` (`LlmProviderConfig` — KHÔNG SecureStore: không nhạy cảm, key vẫn ở keystore); resolver mới `llm_provider_config.dart` cùng hợp đồng không-ném với `AsrEngineSelector` (hỏng/rỗng/thiếu ⇒ Groq mặc định). 3 nơi khởi tạo + chuỗi controller/trigger nhận `llmConfigStore` **tuỳ chọn** (default null = code cũ từng byte). UI: nút "Cấu hình LLM Endpoint/Model (P2.1)" (dialog 2 ô, URL hỏng ⇒ nút Lưu vô hiệu — không lưu giá trị hỏng) + "Khôi phục mặc định Groq" + dòng trạng thái `LLM Endpoint`. **Self-review bắt 1 dead code:** `validateModel` luôn trả null, không caller ⇒ xoá. **Bằng chứng:** analyze No issues found · **320/320** (309 cũ nguyên vẹn + 11 mới: MockClient bắt request thật — chưa cấu hình ⇒ đúng Groq cũ, đã cấu hình ⇒ đúng URL/model mới, đổi giữa 2 lần gọi ⇒ hiệu lực ngay, complete() P5 cùng hưởng, key vẫn Bearer từ SecureStore) · secret scan diff sạch. Chưa verify máy (gộp K51). Báo cáo: `.plan/P2_1-result.md`. **Chưa commit.**

- [2026-09-23] **OpenSpec full update — 10 baseline specs mới + 1 cập nhật (P1A→P7), 18/18 validate pass.** User hỏi "update full openspec chưa" ⇒ kiểm thật: specs cũ chỉ phủ P0.5 (7 specs, commit `7c8f349`) ⇒ trả lời CHƯA + hỏi hướng; user chọn **baseline specs** (mô tả hành vi ĐÃ implement, không change/proposal). Đã viết: `audio-capture` (mic-only, join theo trạng thái thread A56, releaseRecorder theo chủ sở hữu A54), `conversation-vad` (2 state + ngưỡng 300/1500/0.5 + watchdog F2), `asr-engines` (interface chung + PhoWhisper chunk 4s drop-policy + Vosk streaming + selector theo `meta`), `transcript-store` (schema v2 + migration + ghi thẳng đĩa + recovery + purge 7 ngày + detach-race regression), `tts-safety` (fail-closed + 2 lớp mất tai nghe + confirm sau reconnect + thế hệ WAV K45/A54 + speakingChanges — ghi rõ K37 là fail-closed gánh K36, KHÔNG bỏ cổng), `emergency-phrase` (đường thẳng không qua Policy/LLM + xoay vòng + giữ đúng 2s), `suggestion-engine` (prompt khung NGUYÊN VĂN + policy chặn cứng + anti-repetition 2′ + Groq/SecureStore/key-không-log + parser `is`-an toàn A50), `trigger-output` (1 điểm vào duy nhất + output mode fallback an toàn + cache CHỈ fallback + delivery không ném), `pipeline-session` (thứ tự mở/đóng đối xứng + half-duplex thi hành thật + phục hồi từng module + số đo per-session), `coaching` (Pre-Brief nháp `meta` + summary token thế hệ H1-H3 + post-review 3 mục không persist K50 + Training Level thủ công trong Policy + stats không đề xuất), `ethics-reminder` (ghi flag SAU xác nhận R1 + gate không ném); cập nhật `runtime-permissions` (+`BLUETOOTH_CONNECT` nhu cầu thật, `POST_NOTIFICATIONS` không tồn tại trên Android 12 — bằng chứng buổi adb). Mỗi requirement có scenario với nguồn sự thật `file:line` + nợ máy (K37/K41/K45/K49) ghi ngay trong scenario để không ai tưởng đã-verify. `openspec validate --specs` → **18 passed, 0 failed** (fix 1 lỗi: ethics-reminder thiếu scenario). Hồ sơ: `.project/openspec.md` mục 1+4.5. **Specs mới CHƯA commit** (cùng lô docs đang chờ).

- [2026-09-23] **Dọn dẹp + đồng bộ tài liệu (KHÔNG đụng code sản phẩm — user giao "tự quyết sao cho an toàn").** **(1) `TESTING.md` viết lại** khớp code hiện tại: sửa đủ **8 lệch G1–G8** đã liệt kê ở `.plan/P7-result.md` §8 — engine đổi **KHÔNG** tự bật lại (phải bấm "Bật lắng nghe"); UI in `USER_SPEAKING` (tên enum, không phải lỗi); chunk mặc định **4 giây** chứ không phải 12s + nhắc giá trị cũ còn **dính** trong bảng `meta`; quyền cần thêm **`bluetoothConnect`** (thiếu ⇒ TTS không ra tai nghe); **Push thật** = tap nút nổi + API key + Level ≤3, còn nút "Đánh dấu Push (P1E)" **chỉ ghi mốc**; bổ sung toàn bộ luồng P2–P7 (TTS an toàn TC1–TC3 + half-duplex, Emergency giữ đúng 2s, Pre-Brief/Cấp độ/Post-Review/Số liệu 7 ngày, lời nhắc đạo đức lần đầu) và **nói thẳng purge 7 ngày KHÔNG quan sát được trong app**. Thêm §12 bảng "bản cũ nói gì / thực tế thế nào" để khỏi tưởng app mất tính năng. **Không commit `TESTING.md`** và **không** tạo bản sao trong `.project/` — user cố ý giữ file này ngoài git (đã loại khỏi mọi commit từ P4), nên tôn trọng, chỉ ghi lại việc nó là **local-only** ở `.project/openspec.md` §6. **(2) Dọn dung lượng (thứ tự an toàn nhất, kiểm trước khi xoá):** xác nhận `spikes/p0_audio/` **được git track** (45 file ⇒ xoá là khôi phục được) nhưng `models/` (133MB) **bị gitignore** ⇒ **không xoá** — đã `cp -a` sang `/tmp/p0spike-models`, **kiểm 17 file/17 file + đúng dung lượng** rồi mới xoá bản gốc; kiểm luôn `ggml-phowhisper-tiny-q5_0.bin` trong spike là **bản sao y hệt** asset đã ship (`sha256` trùng) và `base-q5_0.bin` (53MB, bản **không** ship) **tái tạo được** bằng `spikes/p0_audio/tools/convert_phowhisper.sh` (~10 phút, README xác nhận). Xoá rác tái tạo được: `app/build` (48MB), `.dart_tool`, `.idea`, `__pycache__`, `*.iml`. **`/home`: 185MB → 364MB trống.** Bẫy đã tránh: `tools/__pycache__/verify_models.cpython-312.pyc` là file **được git track** ⇒ xoá làm `git status` bẩn ⇒ đã `git checkout --` **khôi phục**, không tự đổi repo trong lượt dọn. **(3) Ghi lại lý do HOÃN sửa K37** (`.project/openspec.md` §3+§4, `next.md` bước 0): cổng "xác nhận tai nghe" mỗi lần mở app là **fail-closed đang gánh an toàn** thay cho **K36** — app chưa phân biệt được tai nghe A2DP với **loa Bluetooth** (cùng `TYPE_BLUETOOTH_A2DP`), nên bỏ xác nhận ở callback baseline = có thể **đọc tiếng ra loa BT mà người dùng chưa từng đồng ý**. Sửa đúng phải làm **cùng lúc K36 + verify máy thật** (vùng loại trừ Ponytail, rule 12); giá hiện tại chỉ là **một lần bấm thêm mỗi lần mở app**. **(4) Dọn 5 câu hỏi lỗi thời** ở `.project/openspec.md` §5 (repo + 2 workflow đã có; P0/P0.5 + các phase đã commit; chưa signing là chốt của P7) và ghi rõ `.plan/` + `TESTING.md` là file **local-only**. **Kiểm chứng:** `flutter analyze` **No issues found** · `git status` chỉ còn `TESTING.md` (untracked, cố ý) · **0 file trong `lib/`/`test/`/`android/` bị sửa**.

- [2026-09-23] **P7 — Production Hardening (phần tĩnh xong; signing CHƯA theo chốt user; phần máy thật = nợ K51).** Precondition **KHÔNG ĐẠT** (chưa từng có phiên dùng thật nào + `adb devices` rỗng; điều kiện "≥1 tuần ổn định" bất khả thi với repo 2 ngày tuổi) ⇒ tôi dừng và hỏi; user chọn **waive có ghi rủi ro + làm phần tĩnh**, và chốt **chưa signing** (R8 + APK release UNSIGNED trên CI để đo size). **Task 1 bảo mật (5/5 mục có bằng chứng lệnh thật):** 0 key hard-code (grep toàn repo) · key đọc từ `flutter_secure_storage` (Android Keystore) mỗi lần gọi · **không lưu audio** (`WavSink` 0 caller; native chỉ ghi `cacheDir` tạm của TTS, xoá sau phát) · xoá transcript 7 ngày chạy ở bootstrap (`TranscriptStore.init()`) · 8 permission Manifest **đủ 8 người dùng thật**, không bỏ được cái nào · `.speak(` đúng 3 nơi đều qua `SafeTtsOutput`, không dùng `flutter_tts`. **Task 3 xử lý lỗi vòng 3:** rà toàn bộ file chưa từng soi (`MainActivity.kt` đăng ký/gỡ 4 kênh đối xứng F1; `main.dart` bootstrap try/catch từng mảnh; `foreground_service.dart` `start()` trả `false` thay vì ném + `stopWithTask:false`; `permission_gate.dart`, `app_logger.dart`, `app_database.dart` migration giữ DB user) + kiểm callees của 6 hàm async UI P5 đều cam kết không ném (đọc code xác nhận, không suy đoán) — **không tìm thấy lỗi mới**. **Task 4 dialog đạo đức:** `EthicsConfig` + `EthicsGate` (`lib/coaching/ethics_gate.dart`, cùng hợp đồng không-ném với `TrainingLevelStore`) + dialog trong `HomeScreen._maybeShowEthicsReminder()` — flag `meta` `ethics_reminder_shown`, **chỉ ghi sau khi dialog đóng** (kill giữa chừng ⇒ lần sau hiện lại — hướng an toàn), `barrierDismissible:false`; **6 test unit** khoá hợp đồng. **Task 5 release (không signing):** `build.gradle.kts` bật `isMinifyEnabled + isShrinkResources` (comment ghi rõ debug-signed cố ý + đường lên signing thật, không commit password — rule 8); `proguard-rules.pro` viết lại — keep rules **chỉ 3 nhóm** (JNA · `org.vosk.**` đã xác minh package thật từ import, không đoán `org.kaldi` theo bài học A25 · `AsrNative` object JNI duy nhất), mỗi rule có lý do; workflow mới `.github/workflows/build-release-apk.yml` (`assembleRelease` + báo cáo size/ABI + upload `mapping.txt` + tải model Vosk kiểm SHA256 + retry Gradle A31). **Task 6 checklist nghiệm thu: 0 mục tick** — toàn bộ cần máy thật (K51). **Bằng chứng:** analyze **No issues found** · **308/308 test** (+6 ethics_gate +1 smoke test mở rộng cho dialog). **3 lỗi tự gây trong phiên (tự phát hiện + tự sửa ngay):** (1) replacement nuốt mất dòng khai báo `OutputConfig` ⇒ 53 lỗi analyze (mất `modeKey` cho test cũ) — sửa + verify `grep -c` = 1 cho từng symbol; (2) import sai đường dẫn `ConfigStore`; (3) xoá nhầm thân `resetForTest` — cả 3 đều bị analyze/test bắt trước khi báo xong. **DoD: chỉ mục 3 (báo cáo) tick; mục 1-2 chờ máy** — nợ mới **K51** (gộp cùng buổi K34/K39/K41/K42/K46/K47/K48). Giao: `RELEASE_NOTES.md` (mới) + `.plan/P7-result.md`. **CI release (2 vòng fix):** run 1 **FAIL** — R8 chặn `Missing class java.awt.*` từ `Native$AWT` của AAR JNA (dead code desktop, không tồn tại trên Android) ⇒ thêm `-dontwarn java.awt.**` duy nhất có lý do (`fbf74ce`); run 2 XANH nhưng bước kiểm tra tra `mapping.txt` **sai đường** (`android/app/build/…`) — output thật ở `<repo>/build/app/outputs/mapping/release/` (Flutter plugin redirect; xác minh từ missing_rules.txt của run fail) ⇒ sửa workflow (`8670737`); **run 3 [35822293928] XANH trọn: APK release 70.9 MB (13 .so, ABI arm64-v8a duy nhất) + mapping.txt 76.716 dòng upload artifact.** So sánh: debug 114.9 MB ⇒ **R8 cắt ~44 MB** phần code/resources. Xem `.plan/P7-result.md`.

- [2026-09-23] **P7 review vòng 2 (user hỏi "review p7") — 4 lỗ hổng tự tìm & sửa (chưa commit).** (R1) `barrierDismissible:false` **không** chặn nút back hệ thống ⇒ code cũ gọi `markShown` sau khi dialog đóng với `null` ⇒ một lần bấm back **nuốt vĩnh viễn** lời nhắc đạo đức ⇒ nay `showDialog<bool>` + chỉ mark khi `acknowledged == true`; thêm test khoá và **đã kiểm test biết đỏ** (tạm vô hiệu điều kiện ⇒ fail đúng case). (R2) tên artifact CI nói `unsigned` nhưng APK là **debug-signed** ⇒ đổi `app-release-apk-debugsigned-test` + sửa RELEASE_NOTES (bài học **A58**). (R3) workflow release chạy cả khi chỉ sửa `lib/**` (Dart AOT-compile ngoài R8 ⇒ không thể hỏng keep rules) ⇒ bỏ khỏi `paths`, tiết kiệm ~6 phút CI mỗi push Dart. (R4) xoá `debug { isMinifyEnabled = false }` (no-op). Kèm `scrollable: true` cho dialog. **Bằng chứng:** analyze No issues found · **309/309 test** · diff 4 file. Bài học **A59** (đường dẫn output của Gradle/Flutter phải lấy từ log của chính tool — vụ `mapping.txt`). **Đối chiếu `TESTING.md` với code ⇒ 8 lỗ hổng** (G1 engine không tự bật lại; G2 UI in `USER_SPEAKING`; G3 default chunk 4s không phải 12s; G4 thiếu `bluetoothConnect`; G5 Push thật cần nút nổi + API key + Level ≤3; G6 thiếu toàn bộ P3–P7; G7 purge 7 ngày không quan sát được trong app; G8 guide viết cho bản cách main 21 commit) — **`.plan/HOW_TO_RUN.md` không tồn tại** nên chờ user xác nhận tài liệu nào cần cập nhật. Chi tiết: `.plan/P7-result.md` §6–§8. **Đã commit `f452770`** (`fix(p7)`) + push ⇒ **CI xanh cả hai**: debug OK; release run [35823691989](https://github.com/hoangsoft90/ai_assistant_phone/actions/runs/35823691989) ✅ với artifact **đã đổi tên `app-release-apk-debugsigned-test`** (70.8 MB) + `release-mapping-txt`. **Audit `.plan/HOW_TO_RUN.md`** (user vừa tạo — là tài liệu quy trình copy-paste, KHÔNG phải hướng dẫn test máy) ⇒ đối chiếu từng snippet với `prompt_<PHASE>.md` phát hiện **6 lỗ hổng, đã sửa**: (1) P1E thiếu điều kiện **P1C**; (2) P2 thiếu điều kiện **P1B**; (3) P4 bỏ vế **"pass DoD riêng"**; (4) P5 bỏ vế **"vài phiên test thật"** (chính 2 vế (3)(4) là gốc của 3 lần waive liên tiếp); (5) placeholder `[dán]` ở P3; (6) lỗi HTML `&amp;` ở tiêu đề P7/checklist. Thêm mục **"Khi DoD không đạt"** ghi lại quy trình waive thực tế + cảnh báo snippet là bản rút gọn. `.plan/` bị gitignore ⇒ các sửa này nằm local.

- [2026-09-23] **P5 — Pre-Brief + Session Summary + Post-Review + Training Level (commit `4e25b74`, đã push, **CI xanh** — run 35816046451, artifact `app-debug-apk` 114.9 MB; 302/302 test, analyze sạch).** Precondition P5 **KHÔNG ĐẠT** (`adb devices` rỗng + P4 mới có 0/6 mục DoD tick — chưa phiên test thật nào) ⇒ tôi dừng và hỏi; user chọn **waive có ghi rủi ro** + làm phần không phụ thuộc máy. **1 mâu thuẫn tài liệu đã hỏi & chốt:** `prompt_P5.md` task 3.1 đòi gửi transcript lên **ASR cloud** (Groq Whisper/Deepgram) — trái ràng buộc cứng **#4** (*audio hội thoại không được rời máy*) và không có audio để gửi (app **cố ý** không ghi audio; `WavSink` là tiện ích test) ⇒ user chốt **chỉ dùng transcript local (text) → LLM**; không thêm `connectivity_plus`, không chặn Wi-Fi, không phát sinh dữ liệu lưu trữ mới (nên hạn 7 ngày của P1E không phải mở rộng) ⇒ DoD-3 "không áp dụng, có lý do". **Task 1 Pre-Brief:** `lib/coaching/pre_brief.dart` (6 trường + `ConversationStyle` + `PreBriefStore`; RAM = phiên hiện tại, **nháp** trong bảng `meta` — không schema mới, không migration) + `lib/ui/pre_brief_screen.dart`; nối vào prompt qua `SuggestionContextBuilder.build(preBrief:, summary:)` (thay 2 giá trị mock rỗng của P2); chủ đề kiêng kỵ ghi rõ `TRÁNH (...)` để LLM không đọc ngược; khi mở app bản nháp được đưa làm Pre-Brief của phiên đang chuẩn bị. **Task 2 Session Summary:** `lib/coaching/session_summary.dart` + `TranscriptStore.sessionTranscript()` (cả phiên, kèm `segmentCount`/`truncated`); nhịp **4 nudge HOẶC 5 phút**, gọi **không `await`** (không làm nudge tới muộn), lỗi ⇒ giữ bản cũ + `lastNote`, **không log nội dung**. **Task 3 Post-Review:** `lib/coaching/post_review_service.dart` + `post_review_screen.dart` — đúng **3 mục** (`good/missed/exercise`), parse chịu code fence, mọi nhánh lỗi → `unavailable(note)`; định dạng lạ ⇒ vẫn cho đọc **văn bản thô**; transcript chi tiết **ẩn** sau "Xem chi tiết"; nút "Kết thúc buổi" `stop()` trước rồi mới phân tích. **Task 4 Training Level:** `training_level.dart` (5 cấp + store, ghi lỗi ⇒ không đổi RAM) + **luật đặt trong `SuggestionPolicy`** (2 cổng TRƯỚC LLM: Level 4/5 ⇒ `NO_SUGGESTION` có chủ đích; Level 2 cần ngữ cảnh rõ; Level 3 chỉ khi im lặng ≥ 8s) — cố ý **không** fallback cache; Emergency **không** bị chặn; `weekly_stats.dart` + `stats_screen.dart` (số buổi, Push, **Push/buổi**, 7 ngày, xu hướng) — **không** có logic tự đề xuất đổi cấp. **3 lỗi High tự tìm khi review:** (H1) tóm tắt thử lại **mỗi nudge** khi LLM hỏng ⇒ ghi mốc **lần THỬ** thay vì lần thành công; (H2) nhịp lấy số nudge từ `SessionMemory` (**trần 20**) ⇒ đứng yên sau nudge 20 ⇒ `SuggestionService` giữ bộ đếm riêng; (H3) kết quả tóm tắt **bay về sau `reset()`** ⇒ phiên mới thừa hưởng ngữ cảnh buổi trước ⇒ sửa bằng **token thế hệ** (cùng họ K45/A54), test dùng `Completer` ép LLM trả về muộn. Kèm 1 lỗi tôi tự gây bị **smoke test bắt ngay** (dropdown tràn ngang 47px ⇒ item chỉ hiện label + `helperText`). **Bằng chứng:** analyze sạch · **302/302 test** (+66: pre_brief 11, training_level 16, session_summary 18, post_review 13, weekly_stats 8) · `onSuggestRequested` vẫn **1 nơi gọi** · `.speak(` vẫn 3 nơi hợp lệ · grep vùng P5: **không** file/audio/upload · `git status`: không file nào trong `lib/audio/` bị sửa · dead-code: `runCount`/`updatedAt` nay hiện trên dòng `Coaching (P5)`, `setCurrentForTest` đã xoá. **DoD: 0/5 tick** (đều cần máy thật + API key Groq). Nợ mới **K48/K49/K50**. Xem `.plan/P5-result.md` + `.project/modules/coaching.md`. **Việc kế tiếp theo user chốt: chờ buổi test máy để tick DoD (K48).**

- [2026-09-23] **P4 — Full Pipeline Integration + half-duplex (code + 25 test mới xong, 236/236 pass, analyze sạch; chưa verify máy thật).** Precondition của P4 **KHÔNG ĐẠT** (đối chiếu từng `*-result.md`: P0 0/5, P0.5 3 mục chưa xác minh, P1A 0/5, P1B 0/4, P1C 0/4, P1D 2/4, P1F `[~]`, P1G `[~]`, P3 1/5 — chỉ P1E đạt trọn) ⇒ tôi **dừng và hỏi** thay vì tự quyết; user chọn **waive có ghi rủi ro** + làm **phần không phụ thuộc máy**, hoãn vòng test 30′. **Task 1–5:** `lib/services/conversation_session_controller.dart` (mới) — **orchestrator duy nhất**: mở (service→capture→VAD→ASR) / đóng đúng thứ tự ngược; **half-duplex** = chặn chunk vào ASR khi `SafeTtsOutput` đang phát, tự mở lại khi native báo `spoke`; race Push-khi-đang-phát ⇒ **bỏ qua** (không cắt câu đang đọc giữa từ, KHÔNG cooldown — ràng buộc xuyên phase); Emergency **không** bị chốt đó chặn; phục hồi từng module (mic chết / cả 2 engine ASR fail / feed lỗi 3 lần ⇒ restart tối đa 2 rồi hạ cấp / Trigger lỗi lạ) — tất cả kèm thông báo cho user, không bao giờ ném; `phase` suy ra từ trạng thái thật (idle/listening/processing/speaking) chứ **không lưu song song** (lệch pha = chốt hành xử sai). **Tín hiệu half-duplex:** `SafeTtsOutput.speakingChanges` (stream mới, **không đổi logic an toàn P1F**); cửa sổ báo hiệu rộng hơn thời gian có tiếng (bật khi native *nhận* yêu cầu tổng hợp) — hướng an toàn có chủ ý. **Đo lường cho DoD:** `chunksDroppedWhileSpeaking` / `asrResumeCount` / `overlapPreventedCount` / `averagePushLatency` (định nghĩa **giống P1G**: tới lúc native *bắt đầu tổng hợp*) / `sessionStartedAt` + `recoveryCount`, hiện hết lên dòng `Phiên (P4)` trên màn hình chẩn đoán (bề mặt bằng chứng trên máy, không phải đọc logcat). **Nối UI:** `home_screen.dart` bỏ toàn bộ wiring cũ (‑200 dòng: `_startAsr/_stopAsr/_feedAsr/_asrChunkSub/_asrTicker` + handler lỗi mic) → chỉ còn bấm nút + hiện trạng thái; các module con truy cập qua phiên (`_asrKind`/`_trigger`/`_transcript`/`emergency` là getter) để **không có hai instance song song**. **Bằng chứng:** analyze sạch · **236/236 test** (+25: half-duplex chặn/mở lại chunk, Push bị bỏ qua + KHÔNG cooldown, emergency không bị chặn, 6 ca phục hồi, độ trễ + chuỗi pha, vòng đời + đổi engine + suy ra engine THẬT khi fallback) · `grep -rn onSuggestRequested lib/` còn **đúng 1 nơi gọi** (trong phiên, không ở UI) · `.speak(` vẫn chỉ 3 nơi hợp lệ (SafeTtsOutput) · controller **không** import network/LLM/kênh native. **2 điểm tự tìm khi review đã sửa:** rò subscription stream transcript của ASR + số đếm tích tụ qua nhiều phiên; bỏ `lastPushLatency` không có caller (A8). **1 phát hiện HIGH loại native (K45 — ĐÃ SỬA theo yêu cầu user, xem dưới):** `SafeTtsBridge` dùng **chung field `tempWav`** cho hai "thế hệ" ⇒ `finally { cleanTemp() }` của lần phát cũ xoá file của câu **mới** ⇒ câu mới **im lặng** (`không có file WAV để phát`); vì tổng hợp TTS lâu hơn thời gian thread cũ thoát, đây gần như **tất định** chứ không phải race hiếm, và ảnh hưởng đúng đường **Emergency Phrase khi đang đọc nudge**. **Fix K45 (user chốt "sửa luôn trong commit P4")**: `wavFor(gen)` thành **nguồn duy nhất** cho đường dẫn/tên file WAV, `playSynthesized` giữ file của **chính lần chạy** rồi `cleanTemp(wav)`, `cleanTemp(file)` **chỉ null field nếu `tempWav === file`**, `onError` suy thế hệ từ `utteranceId` (`cleanTempOfGeneration`) — call-site thứ hai cùng họ lỗi, chỉ lộ ra khi rà hết mọi chỗ thế hệ cũ còn chạm state dùng chung (A54). Kotlin **không biên dịch được ở máy dev** (không Android SDK) ⇒ chờ CI; **hành vi** còn phải nghe trên máy (bước 7 giáo trình test) nên K45 giữ ở mức 🟠, chưa đóng. Nợ mới **K45** (lỗi native trên), **K46** (5 mục DoD máy thật P4); **K35 (half-duplex chưa nối) đóng ở tầng code**. **Commit `00e16e0`** `feat(pipeline)` — 17 file (code + test + docs; `TESTING.md` của bạn không đưa vào), đã push `ccdaaac..00e16e0` để CI biên dịch Kotlin (lần này có sửa native: `speak()`/`cleanTemp`/`wavFor`) → **CI XANH** (run [35808819851](https://github.com/hoangsoft90/ai_assistant_phone/actions/runs/35808819851), artifact `app-debug-apk` 114.8MB, hết hạn 2026-10-23) ⇒ bản sửa K45 **biên dịch được**; việc còn lại là xác nhận hành vi trên máy. **Review lại chính bản sửa (theo bước Code Review)** ⇒ kiểm kê hết state dùng chung của `SafeTtsEngine` và tìm thêm **2 lỗi CÙNG HỌ, đã sửa (chưa commit)**: (a) `onError`/`onStop` đặt `synthesizing = false` mà **không kiểm thế hệ** ⇒ `onStop` của thế hệ cũ (hệ quả mong đợi của chính `speak()` gọi `engine.stop()`) làm `onDone` của câu MỚI bị bỏ qua ⇒ câu mới im lặng — call-site thứ ba của K45, xác suất CAO chứ không hiếm; (b) `onEvent("error")` của thế hệ cũ báo về Dart ⇒ `SafeTtsOutput._setSpeaking(false)` ⇒ **mở lại cửa ASR giữa lúc TTS đang đọc** (vi phạm half-duplex — đúng DoD 2 của P4) + thông báo "Lỗi đọc TTS" sai. Đã thêm `isCurrentGeneration()` + `handleSynthesisFailure()`, `cleanTemp` so **đường dẫn** (`==`) thay vì `===`. Analyze sạch · 236/236 test pass; **user đã xác nhận ⇒ commit `995c675` + push, **CI XANH** (Kotlin biên dịch được). **Rà tiếp 5 file native ASR/capture/VAD (1.332 dòng) theo cùng cách** (user yêu cầu) ⇒ **1 lỗi CRASH TIẾN TRÌNH + 2 lỗi cùng họ, đã sửa (chưa commit)**: (1) `WhisperChunkEngine.close()` free model khi `nativeTranscribe` còn chạy (use-after-free) **và** `RejectedExecutionException` ném ra từ `finally` (Android giết tiến trình) — ca thật là **dừng nghe đúng lúc đang transcribe** ⇒ nay `awaitTermination(5s)` trước khi free + `submit()` trả `Boolean` tự bỏ qua khi engine đóng; (2) `MicCaptureEngine` nhánh lỗi nhả `AudioRecord` theo field dùng chung ⇒ có thể nhả mất recorder MỚI (nay `releaseRecorder(recorder)` theo chủ sở hữu — A54); (3) `stop()` bỏ qua join thread đã tự dừng vì lỗi ⇒ `release()` đóng VAD dưới chân thread còn `analyze()` (nay join theo trạng thái thread, trả cờ để bỏ qua đóng native; `record` nay dưới `recorderLock` riêng). Kèm `@Volatile` cho 4 field dùng chung. Rà thấy **sạch**: `VoskStreamingEngine.release()` (chính là mẫu cho fix 1), `VadDetector`, `CaptureChannelBridge`. Bài học mới **A55/A56**; nợ **K47**. Analyze sạch · 236/236 test pass (native không có test harness ⇒ chờ buổi test máy). User cho phép commit ⇒ **`b8327bf`** + push → **CI XANH** ([run 35810909445](https://github.com/hoangsoft90/ai_assistant_phone/actions/runs/35810909445)) ⇒ **cả 3 file Kotlin biên dịch được**; việc còn lại chỉ là xác nhận hành vi trên máy (K47). Xem `.plan/P4-result.md` + `.project/modules/pipeline-integration.md`.

- [2026-09-22] **P3 — Trigger Abstraction + Output Mode + Offline Nudge Cache (code + 50 test mới xong, 211/211 pass, analyze sạch; chưa verify máy thật).** Làm theo `.plan/prompt_P3.md` **phần không phụ thuộc máy** (user chọn sau khi tôi báo Precondition chưa đạt: P2 chưa từng có nudge thật vì chưa có UI nhập Groq key, P1F còn nợ TC2/TC3, `adb devices` rỗng ⇒ không thể tự test). **Task 1:** `lib/trigger/trigger_manager.dart` — `onSuggestRequested(source)` là **điểm vào DUY NHẤT** cho mọi nguồn (grep: chỉ 1 nơi gọi trong `lib/`), `SuggestTriggerSource` sẵn 5 giá trị (nút nổi/nút chẩn đoán/thông báo/volume key/nút BT), không cooldown cho Push thủ công, mốc Push lỗi không chặn Push. **Task 2:** `lib/ui/floating_button.dart` — tap = Push, **giữ đúng 2s** = Emergency (P1G) đi thẳng, không qua LLM/Policy/debounce. **Task 3:** `output_mode_selector.dart` (ear/haptic/silent lưu bảng `meta`; Ear **tự hạ xuống chữ** khi thiếu tai nghe; tốc độ đọc 0.9-1.2x mặc định 1.05x nối tới `setSpeechRate`) + `nudge_delivery.dart` (đọc/rung/chữ, không bao giờ ném; rung lỗi ⇒ hạ xuống chữ). **Task 4:** `assets/offline_nudge_cache.json` (72 câu = 12 × 6 type, mọi câu **2-4 từ**) + `lib/suggestion/offline_nudge_cache.dart` (xoay vòng, tránh câu vừa hiện, **không bao giờ ném**) — chỉ fallback khi **không dùng được LLM** (Policy chặn hoặc `NO_SUGGESTION` hợp lệ thì KHÔNG), có đánh dấu nguồn `NudgeSource.cache` + `cacheFallbackCount` + dòng `CACHE OFFLINE` trên màn hình. **Task 5:** nút chẩn đoán cũ đi cùng đường với nút nổi; thêm khu Settings tối thiểu (chế độ + thanh trượt tốc độ + **nút nhập API key Groq** — P2 chỉ *đọc* key từ `SecureStore` mà không có chỗ *ghi*, nên DoD-1 không thể chạy trên máy nếu thiếu nút này). **6 lỗi tự tìm khi review và sửa:** (1) ngưỡng giữ bị lệch +100ms do `GestureDetector.onTapDown` chỉ bắn sau `kPressTimeout` ⇒ viết lại bằng `RawGestureDetector` + `LongPressGestureRecognizer(duration: 2s)`, bỏ ~40 dòng timer thủ công và **test được đúng mốc 2s**; (2) nội dung nudge lọt vào logcat qua `TriggerManager` log `$result` ⇒ thêm `SuggestionResult.logLabel` (không có nội dung) cho mọi log; (3) `dispose()` `TextEditingController` ngay sau `showDialog` ⇒ dùng `onChanged` + biến cục bộ; (4) `argument<Double>("rate")` ở Kotlin quá giòn (gửi số nguyên ⇒ `ClassCastException` ⇒ **mất cả tiếng đọc**) ⇒ đọc qua `Number` + kẹp; (5) `OfflineNudgeCache` nạp lại có thể **nhân đôi type** ⇒ xoá sạch trước khi nạp + test regression; (6) rung lỗi làm mất cả nudge chữ ⇒ bắt lỗi và hạ xuống chữ. **Bằng chứng:** `flutter analyze` sạch · **211/211 test** (+50: `floating_button_test` 9, `trigger_manager_test` 16, `offline_nudge_cache_test` 13, `output_mode_selector_test` 11, +1 test tốc độ đọc trong `safe_tts_output_test`) · test asset validate đủ 6 type × 12 câu × 2-4 từ · grep cách ly (vùng P3 không có network) · `grep -rn "\.speak(" lib/` vẫn chỉ 3 nơi hợp lệ · dead-code check 11 symbol mới đều có caller. **Sai khác có lý do:** nút nổi **trong app**, KHÔNG xin `SYSTEM_ALERT_WINDOW` (overlay + phát TTS nền chạm đúng K36/P4); volume key / nút tai nghe BT / notification action **chưa implement** (kèm lý do kỹ thuật + đường nối sẵn 1 dòng) — DoD-5 cho phép; thêm nút nhập API key; mặc định `ear` (silent sẽ làm app cài xong không đọc gì); "Silent không phụ thuộc mạng" hiểu là *đường giao nudge* không cần mạng (Push vẫn hỏi LLM). Nợ mới **K41** (tốc độ đọc chưa verify native), **K42** (buổi test máy thật P3), **K43** (trigger ngoài app), **K44** (cache không theo chủ đề); **K38/K40 đóng**. **Chưa commit** (vùng audio — rule 12). Xem `.plan/P3-result.md`.

- [2026-09-22] **P2 — Suggestion Engine (LLM + Policy) — code xong, 161/161 test, chưa test máy thật.** **Review lần 2 (user yêu cầu):** tìm thêm **3 lỗi High** bằng cách viết test phá code — cùng một họ "dùng `as` ép kiểu dữ liệu bên ngoài ⇒ `TypeError` xuyên qua `on SuggestionException` ⇒ `push()` NÉM ra UI": (H1) parser `type: 123`; (H2) provider envelope dị dạng (`choices` là chuỗi...); (H3) service chỉ bắt `SuggestionException`. Đã sửa bằng đọc-kiểu-`is` + thêm `_generateOnce()` chuẩn hoá mọi lỗi provider; 4 test regression mới. Sửa thêm 2 Low: không log nội dung nudge (chỉ log loại) + test fake `noSuchMethod` ném thay vì trả `null`. Bài học **A50**. `lib/suggestion/`: `suggestion_models.dart` (NudgeType 6 loại mục 4.5, `SuggestionResult`, `SuggestionContext`, `SuggestionException` có cờ `retryable`, `parseSuggestionOutput` 2 dạng hợp lệ + bóc code fence) · `llm_provider.dart` (`abstract class LlmProvider` đúng chữ ký prompt — thêm Gemini sau không phải sửa tầng trên) · `groq_llm_provider.dart` (Groq `llama-3.1-8b-instant`, endpoint OpenAI-compatible xác minh từ tài liệu 2026-09, `response_format: json_object`, timeout 4s, key đọc từ `SecureStore` **mỗi lần gọi**) · `suggestion_policy.dart` (chặn CỨNG `userSpeaking` **trước** khi dựng context, debounce 1s — KHÔNG cooldown, anti-repetition text/type trong 2 phút) · `session_memory.dart` (recentSuggestions/topicsExplored/lastNudgeType trong RAM) · `suggestion_context_builder.dart` (**prompt khung NGUYÊN VĂN** mục 4 prompt P2, chỉ thay `{placeholder}`) · `suggestion_service.dart` (orchestrator; `push()` **không bao giờ ném**). Thêm `http` vào pubspec; `SuggestionConfig` vào constants. UI tạm: nút "Xin gợi ý (P2)" (tự ghi mốc Push rồi xin gợi ý) + dòng `Gợi ý (P2)` trên màn hình chẩn đoán. **Bằng chứng:** analyze sạch · **161/161 test** (25 mới + 4 regression review) · **prompt khung khớp từng dòng** với `.plan/prompt_P2.md` bằng script so sánh · grep cách ly (không import ngoài dự kiến, không audio, không key cứng). **Sai khác có lý do:** (1) thêm `suggestion_service.dart` ngoài danh sách bàn giao của prompt — cần một nơi duy nhất nối Policy → Context → LLM → parse cho tầng UI gọi; (2) retry **chỉ** cho lỗi JSON (cờ `retryable`), timeout/mất mạng fail ngay để giữ mục 6 "NO_SUGGESTION sau ~3-4s"; (3) `{pre_brief}`/`{summary}` để `(chưa có)` thay vì chuỗi rỗng cho LLM dễ đọc. **Lỗ hổng tự phát hiện khi review:** `push()` gọi `recentWindow()` có thể ném (SQLite lỗi) trong khi doc khẳng định không ném ⇒ đã bọc + thêm test "transcript lỗi ⇒ KHÔNG ném". Nợ **K39** (5 mục DoD cần máy thật + Groq API key trong SecureStore) + **K40** (Offline Nudge Cache — P3). **Chưa commit.** Xem `.plan/P2-result.md`.

- [2026-09-22] **P1G — Emergency Phrase (code xong, 130/130 test).** `lib/audio/emergency/`: `emergency_phrases.dart` (file cấu hình riêng, 3 câu của prompt) + `emergency_phrase_service.dart` — `triggerEmergency()` chọn câu **xoay vòng** (lý do trong code: dự đoán được, không lặp liền, không cần random), gọi thẳng `SafeTtsOutput` (0 network/LLM — grep chứng minh), đo `lastTriggerToSynthLatency` (trigger→native bắt đầu tổng hợp, đúng định nghĩa DoD). Nút tạm + dòng `Emergency` trên màn hình chẩn đoán; gesture thật là P3. 8 test mới (không tai nghe ⇒ 0 lần gọi native speak + 1 rung; xoay vòng; failed không retry; lọc câu rỗng). Sửa smoke test: màn hình dài hơn ⇒ phải cuộn tới dòng trạng thái (ListView lazy). Precondition P1F: **đạt một phần có hướng dẫn của user** (buổi test qua adb: phát đầu-cuối ✅ user nghe rõ, mất-tai-nghe+rung ✅; còn nợ rút-mid/TC2/TC3 — K34 thu hẹp; phát hiện **K37**: callback baseline của `registerAudioDeviceCallback` bị coi là "kết nối lại" ⇒ đòi xác nhận mỗi lần mở app). Bài học **A47** (test-via-adb: content-desc, dump 2 lần, doze nuốt tap). **Chưa commit** (vùng audio, rule 12) — chờ user.

- [2026-09-22] **Buổi test P1F trên máy thật qua adb (một phần, user dừng giữa chừng).** Pixel 3a + APK run #24 (`310f2be`, xác nhận bằng mốc cài 15:06 +07 sau 07:47 UTC) + tai nghe dây. Đã xác minh: phát TTS đầu-cuối (`đã phát 111946/111946 byte`, user **nghe rõ**, SCO inactive), phát hiện mất tai nghe + rung 2 nhịp (`MẤT thiết bị riêng tư: 4` → `đã rung: HEADSET_LOST`, 9ms), chuỗi cắm→khoá→Xác nhận→phát, chặn phát khi chưa xác nhận. Chưa trúng: rút giữa lúc đang phát (2,5s tổng hợp im lặng làm lỡ nhịp 2 lần), TC2, TC3 (không tai nghe BT). Bằng chứng chi tiết: `.plan/P1F-result.md` mục "Cập nhật sau buổi test qua adb". Giáo trình thao tác đã rút kinh nghiệm thành A47.

- [2026-09-22] **P1F — TTS Output Safety Layer (code xong, 3/4 mục DoD chưa thể tick).** `lib/audio/tts/` (`safe_tts_output.dart` + `tts_client.dart` + `tts_channels.dart`) bọc `SafeTtsBridge.kt` (native): Dart đọc **tươi** trạng thái tai nghe trước mỗi lần đọc ⇒ không có tai nghe thì **không gọi phát**, chỉ rung + nudge chữ; có tai nghe ⇒ `TextToSpeech.synthesizeToFile` (chỉ tổng hợp) rồi tự phát PCM qua `AudioTrack.setPreferredDevice(<tai nghe>)`, `USAGE_MEDIA`. Hai lớp phát hiện mất kết nối: `ACTION_AUDIO_BECOMING_NOISY` (sớm nhất, hệ thống bắn TRƯỚC khi đổi route) + `AudioDeviceCallback` (bảo hiểm, phủ cả Bluetooth); mất giữa chừng ⇒ dừng ngay + rung 2 nhịp + im lặng cho tới khi user xác nhận. Generation token chặn phát file tổng hợp xong muộn (mẫu race A33/K23). Manifest thêm `<queries> TTS_SERVICE` + `VIBRATE`; `PermissionGate` xin `BLUETOOTH_CONNECT`; màn hình chẩn đoán thêm dòng `TTS` + 2 nút tạm. **Sai khác có lý do so với prompt:** dùng TextToSpeech native thay `flutter_tts` (đúng `openspec/config.yaml`, không thêm dependency), `USAGE_MEDIA` thay vì `USAGE_VOICE_COMMUNICATION` (usage đó bị cấm vì kéo HFP/SCO), `AudioDeviceCallback` thay `ACTION_ACL_DISCONNECTED`. Analyze sạch · **122/122 test** (21 mới) · commit `37b6ef1` + `386c3df`, CI run #22/#23 XANH (Kotlin biên dịch được) — **3 test case bắt buộc trên máy thật còn nợ (K34)**: cần APK + tai nghe, không được tự nhận đã test.

- [2026-09-22] **Fix `_selectAsrEngine`: đổi engine ASR khi đang thu làm app lỗi** (user báo). Đổi engine giờ tắt hẳn theo đúng chuỗi của nút "Tắt lắng nghe" (ASR + detach transcript → VAD → capture → service), ghi cấu hình rồi để user tự bật lại; không tự start lại (đó là nguồn lỗi: `dispose()` engine cũ trong lúc capture còn bơm chunk). Nếu ghi config lỗi thì giữ engine cũ để không chặn lần bấm lại. Commit `4bb3da0`. Analyze sạch · test pass.

- [2026-09-21] **P1E — Transcript Store (code xong, 2/4 mục DoD đạt).** `lib/transcript/` (`transcript_segment.dart` **chỉ có text + timestamp**, `transcript_store.dart`) + `lib/services/storage/transcript_dao.dart` (interface + bản SQLite, cùng mẫu `ConfigStore` của P1D nên test được bằng DAO giả). **Schema SQLite lên v2** (3 bảng `transcript_sessions/segments/pushes` + 2 index) + nhánh migration `v1 → v2` cho máy đã cài P0.5 — DDL trong code được **kiểm thật bằng sqlite3 của Python** trên một DB v1 giả (bảng/index đúng, `meta` cũ còn nguyên). Ghi thẳng xuống đĩa từng dòng (không gom lô) ⇒ khôi phục được sau khi bị kill; cửa sổ RAM 8 phút, xoá dữ liệu cũ hơn 7 ngày ở `init()` (transaction 3 bảng). Nối vào app: `main()` init (recovery + cleanup không phụ thuộc thao tác người dùng) + `home_screen` attach/detach theo vòng đời ASR, thêm dòng "Transcript"/"Push gần nhất" và nút Push tạm để kiểm trên máy. **Bug tự phát hiện qua test:** `attach()` gọi `unawaited(detach())` ⇒ detach treo ở `await` rồi `_asrSub = null` đè lên subscription mới ⇒ **stream cũ vẫn ghi transcript** (họ lỗi race như F2/K23) — đã sửa (capture-trước-khi-xoá + huỷ trên biến cục bộ) + test regression. Cũng sửa: `init()` cache future hỏng ⇒ store chết vĩnh viễn nếu SQLite lỗi thoáng qua. Analyze sạch · **95/95 test** (16 mới). Nợ: K27 (đo crash/7 ngày trên máy thật), K28 (chốt có dùng SQLCipher mã hoá DB không).
- [2026-09-21] **CI `35675870025` (`bb436a3`) XANH** — icon launcher mới (adaptive icon + PNG legacy + `roundIcon`) build APK thành công ⇒ resource Android biên dịch được (đây là mục verify duy nhất cho thay đổi thuần resource; xem A32).
- [2026-09-21] **Launcher icon mới (concept user chốt: bong bóng hội thoại + sóng âm, tông teal).** Sinh bằng script tái tạo được `tools/make_app_icon.py` (Pillow, không sửa tay PNG) → **adaptive icon** `mipmap-anydpi-v26` (minSdk 26 ⇒ mọi máy đi nhánh này) + PNG legacy đủ 5 dpi + `android:roundIcon` trong manifest. **Lỗi tự phát hiện khi rà lại:** bản đầu vẽ luôn gradient vào layer `foreground` (đục) ⇒ launcher không áp được mặt nạ/parallax và `ic_launcher_background.png` thành file chết; đã sửa thành **foreground trong suốt** + background là layer riêng, xoá `values/ic_launcher_background.xml` không còn ai tham chiếu. Hình học kiểm bằng số: chóp đuôi cách tâm **30.2dp < 33dp** vùng an toàn (mặt nạ tròn không cắt đuôi). Ràng buộc + bảng màu ghi vào `.project/design-system.md`. Preview 4 ô: `icon_preview.png` (gitignore). analyze sạch · 79/79 test. Việc verify **thật** là build APK trên CI (local không có Android SDK) — bài học **A32**. Lỗi phụ: Flutter SDK local phải tải lại Dart SDK (233MB) do bị SIGTERM giữa chừng (A32).
- [2026-09-21] **Fix CI run `35618380022` (fail trên commit docs `c6a2979`).** Gốc: Gradle wrapper tải distribution `gradle-9.3.1-all.zip` nhận **HTTP 504** từ services.gradle.org — lỗi hạ tầng thoáng qua, KHÔNG phải code. Fix: retry 3 lần bước tải distribution trong workflow + đổi `-all` (341MB) → `-bin` (129MB). Dọn luôn 2 lỗi analyze của spike trên CI (`spikes/**` vào exclude — log CI sạch). Bài học **A31** (phân loại fail: hạ tầng vs code trước khi sửa). analyze sạch · 79/79 test.
- [2026-09-21] **Review P1D → sửa F1–F5 (`2c0ae82`, `e6f01ac`) → CI `35617319912` XANH.** F1/K22: nạp/giải phóng model chạy trên loader thread riêng (hết treo UI/ANR) — áp cho cả bridge P1C. F2/K23: generation token + worker giữ recognizer riêng. F3/K25: `initTimeout` (test 2 engine). F4/K26: flush `getFinalResult()` khi release + Dart gọi release trước khi đóng stream. F5/K24: xoá 3 getter chết. Lần push đầu fail 8 lỗi Kotlin (`No value passed for parameter 'messenger'` — tham số chết trong helper) → bài học **A30** + bỏ tham số chết. analyze sạch · **79/79 test**. Bài học mới A26–A30 + 4 quy tắc đã áp dụng trong skill. P1E **chưa viết code** (mới kiểm precondition + thu thập dữ kiện).

- [2026-09-21] **P1D code xong (`82f91d2` → CI run 35612601371): Vosk làm ASR dự phòng + `AsrEngineSelector`.** Dùng AAR chính chủ `com.alphacephei:vosk-android:0.3.75@aar` + `jna@aar` (KHÔNG dùng `vosk_flutter`: Dart <3 / kéo permission_handler ^13 → compileSdk 37 > AGP 9.1.0 max 36 — 3 bài học A20-A22). Model Vosk 32MB do CI tải + kiểm SHA256, đóng thành **Android asset** (không khai pubspec vì asset thiếu làm `flutter test` fail — A21). Config engine dùng lại bảng `meta` (không thêm dependency Dart). Nối ASR vào màn hình chính (chọn engine + số liệu so sánh). analyze sạch · **76/76 test**. Quyết định engine: **PhoWhisper mặc định (tạm thời)**, lý do + bảng so sánh ở `lib/audio/asr/README.md`. Báo cáo: `.plan/P1D-result.md`. Nợ: K19 (đo máy thật), K20 (JNA trên máy thật), K21 (APK +32MB/RAM model).
- [2026-09-21] **Run #7 (`10eeb5a`) XANH — whisper.cpp + JNI compile/link thành công (K17 đóng, K11 đóng hoàn toàn).** C1: sửa symbol JNI về `Java_com_aiassistant_phone_asr_AsrNative_*` (tôi từng viết sai `phone__asr` — sai `_` vẫn compile pass, chỉ lộ runtime; bài học A19 đã sửa lại trong skill). Commit `7ea1022` → **run #8** là APK để cài máy thật.
- [2026-09-21] **Run #5 fail ở CMake** (`DOWNLOAD_EXTRACT_TIMESTAMP` không hỗ trợ cmake 3.22.1 NDK). Fix + xử lý batch user giao: JNI symbol `_asr_` (lỗi runtime tiềm ẩn — package Kotlin là `…phone.asr`, không thêm thì UnsatisfiedLinkError dù compile pass), `pending` FloatArray? (đã xong `ee34224`), `AsrChannelBridge.unregister` đối xứng F1 (gọi ở `onEngineWillDestroy` + `cleanUpFlutterEngine`). Commit `6861515` + `10eeb5a` → **run #7** (`10eeb5a`) build cả 3 fix. Bài học A18/A19 vào skill.
- [2026-09-21] **Run #3 XANH (APK debug đầu tiên build thành công — K11 Kotlin đóng).** Run #4 (P1C) fail ở compile Kotlin: `pending: ShortArray?` sai kiểu — fix thành `FloatArray?` (commit `ee34224` → run #5), bài học A17 vào skill.
- [2026-09-21] **P1C code xong (user waive precondition) → run #4 (`f29bef5`, in_progress).** Interface `AsrEngine` đúng chữ ký prompt; `PhoWhisperAsrEngine` (accumulator 4s, không mất mẫu, drop-policy giữ chunk mới nhất); JNI/CMake port từ spike P0 (pin commit 307869a, symbol đổi package); model tiny q5_0 (29MB) vào assets; Kotlin bridge `AsrChannelBridge` đăng ký cả 2 engine. analyze sạch + **50/50 test** (11 mới). Đo máy thật GHI NỢ (K18); native lần đầu compile ở run #4 (K17). Báo cáo: `.plan/P1C-result.md`.
- [2026-09-21] **Fix CI run #2 fail → run #3 (`b5b0940`, in_progress).** Tin tốt: JitPack/K14 đã thông (resolve dependency không lỗi) và vòng compile Kotlin đầu tiên (K11) chạy tới — chỉ đúng **1 lỗi**: `MicCaptureEngine.kt:144 Unresolved reference '_config'` (chính tả string template, `$_config` → `$config`). Rà phòng ngừa `grep '\$_...'` toàn bộ Kotlin → sạch. Skill đã ghi bài học: run đầu sau khi qua compileSdk sẽ dồn lỗi Kotlin, đọc từng dòng `e:` trong log.
- [2026-09-21] **Fix CI run #1 fail → run #2.** Nguyên nhân: `permission_handler_android` 14.1.0 (do lock 13.0.2 kéo lên) đòi compileSdk 37 > max AGP 9.1.0 = 36 → fail `checkDebugAarMetadata` (không phải lỗi Kotlin của ta). Fix: pin `permission_handler: 12.0.1` → android 13.0.1 (compileSdk 35), analyze sạch + 41/41 test, commit `282b1a8` pushed, run #2 in_progress. Bài học compileSdk-plugin đã ghi vào skill. **Lưu ý Kotlin (K11) chưa được compile ở run #1** — fail sớm ở AAR metadata, vòng compile Kotlin chỉ chạy ở run #2 trở đi.
- [2026-09-21] **GH repo + CI build APK + skill.** Push commit đầu tiên `7c8f349` lên `main` (repo `hoangsoft90/ai_assistant_phone`, 155 file — đã loại `.plan/`, `.agents/`, `.freebuff/`, model spike). Workflow `build-debug-apk.yml` (gradlew trực tiếp, JDK17, Flutter 3.47.2, tự sinh `local.properties`, artifact 30 ngày) — run #1 đã **in_progress** sau push. Skill `.agents/skills/ai-assistant-phone-debug-apk/SKILL.md` (token + repo lưu tại đây, không hỏi lại; đã test load). Un-ignore `android/gradlew` + wrapper jar (trước đó bị gitignore ⇒ CI không có gradlew). Token KHÔNG vào bất kỳ file nào trong git (quét sạch; `.freebuff/` cũng đã untrack). **P1C tạm dừng ở precondition** (chưa code gì) — chờ quyết định go/no-go PhoWhisper từ user.
- [2026-09-21] **Regression F1 (user phát hiện):** `dispatchChunk` dùng `holder.sink` trên `EngineChannels` (không tồn tại → lỗi compile Kotlin, analyze Dart không bắt được vì K11). Đã sửa 2 chỗ → `pcmHolder.sink`, rà grep toàn file sạch, đối chiếu chéo kênh/payload Kotlin↔Dart, verify lại: analyze sạch + **41/41 test pass**. Bài học ghi ở `.plan/P1B-result.md` + `LESSONS_LEARNED.md`.
- [2026-09-21] **Review code P1B + sửa F1–F5 (bổ sung sau P1B).** Phát hiện 6 vấn đề (2 🔴); user duyệt: F2 = watchdog 1.5s (không reset tức thì), sửa F1/F3/F4/F5, F6 để sau. Kết quả: F1 unregister sạch theo engine (map theo messenger, nhớ messenger service trong MainActivity), F2 watchdog `inputStalled` (⚠ Timer phải hẹn ngay trong `_publish` lúc vào khoá — test bắt được bug này), F3 `frameMs` từ `VadDetector` qua `VadResult` (xoá hằng hardcode), F4 HomeScreen sống theo sự kiện (status/errors/stats + dispose), F5 assert theo mốc onset. Verify: analyze sạch, **41/41 test pass**. Kotlin vẫn chưa từng biên dịch (K11/K14). Chi tiết: `.plan/P1B-result.md` mục "Bổ sung sau review".
- [2026-09-21] **P1B (VAD + State tối giản) — CODE XONG, 0/4 mục DoD đạt (chưa đo được).**
  Precondition không đạt (P1A `0/5 mục DoD`, chưa có APK/thiết bị) → hỏi và được **waive**.
  - Đã làm: WebRTC VAD (`com.github.gkonovalov.android-vad:webrtc:2.0.10` qua JitPack) bọc trong
    `VadDetector.kt` (16kHz, khung 320 mẫu = 20ms, VERY_AGGRESSIVE); VAD chạy **trên thread thu**
    của P1A + kênh `com.aiassistant.phone/vad` (chỉ chạy khi có listener);
    `ConversationStateMachine` (Dart) với **đúng 2 state** + bộ tích luỹ rò (300ms/1500ms/ratio 0.5),
    `ValueNotifier` + `Stream changes/transitions` + `isUserSpeaking` cho P2, lịch sử trong phiên (≤500);
    dòng `Hội thoại` trên màn hình chẩn đoán.
  - **Giải quyết nợ K12**: không cần hạ `chunkMs` xuống 20–30ms — VAD tự chia khung 20ms ở native.
  - Kiểm chứng: `flutter analyze` **No issues found**; `flutter test` **38/38 pass** (17 test mới);
    **timeline hội thoại mẫu** in ra thật: `700ms: notUserSpeaking → userSpeaking` /
    `5900ms: userSpeaking → notUserSpeaking`; đối chiếu API thư viện VAD bằng source thật + artifact
    JitPack HTTP 200 trước khi pin version.
  - **Chưa verify**: phản hồi <500ms với giọng thật, ngừng nói 1–2s, không flicker khi có nhạc/TV,
    chạy 30 phút. **Mã Kotlin + dependency JitPack chưa từng được biên dịch** (nợ K14 🔴). Chưa commit.
  - Báo cáo: **`.plan/P1B-result.md`**. Tài liệu module: `.project/modules/conversation-state.md`.

- [2026-09-21] **P1A (Audio Capture Foundation, mic-only) — CODE XONG, 0/5 mục DoD đạt (chưa đo được).**
  Precondition không đạt (P0.5 chưa verify, chưa có kết luận A2DP) → hỏi và được **waive**.
  - Đã làm: `MicCaptureEngine` (Kotlin, `AudioRecord` + `AudioSource.MIC`, 16kHz mono PCM16, thread
    `URGENT_AUDIO`), `CaptureChannelBridge` (kênh control + pcm, **1 engine dùng chung, sink riêng
    từng FlutterEngine**), `AudioCaptureController` (Dart facade, broadcast stream), interface
    `AudioCaptureEngine`/`CaptureClient`, `WavSink` (kiểm thử thủ công), UI bật/tắt capture kèm
    rollback service khi mic lỗi. Capture chạy trong process do foreground service giữ; kênh **đã**
    đăng ký cho engine của service qua `ForegroundService.addTaskLifecycleListener` để P1B dùng.
  - Kiểm chứng: `flutter analyze` **No issues found**; `flutter test` **21/21 pass** (20 test mới:
    lifecycle, chunk ngoài cửa sổ bị bỏ, map lỗi, onError 1-handler, dispose, hợp đồng kênh thật).
  - Tự review Kotlin tìm & **sửa 2 lỗi thật**: `stop()` join chính thread đọc (treo 1.5s vô ích),
    và `onCancel` xoá chung set sink làm mất listener của engine kia.
  - **Chưa verify**: 60 phút nền, `dumpsys audio` HFP/SCO, rút tai nghe, nghe lại `.wav`, luồng từ
    chối quyền trên UI — đều cần APK + máy thật. **Mã Kotlin chưa từng được biên dịch.** Chưa commit.
  - Báo cáo: **`.plan/P1A-result.md`**. Tài liệu module: `.project/modules/audio-capture.md`.

- [2026-09-21] Viết **baseline spec OpenSpec** cho toàn bộ code hiện có (theo yêu cầu user: chỉ viết
  tài liệu, KHÔNG sửa code): 7 spec trong `openspec/specs/` — `app-bootstrap`,
  `listening-foreground-service`, `runtime-permissions`, `audio-session-config`, `app-storage`,
  `diagnostics-home-screen`, `app-core` (gộp logging+constants+smoke-test theo xác nhận của user).
  Mỗi spec: Purpose + Requirements (PHẢI/MUST) + Scenario Given/When/Then kèm `file:line` + mục
  **Cần làm rõ** (hành vi mơ hồ/dead code — không tự sửa). `openspec validate --specs` → **7 passed,
  0 failed, 0 warning**. Đã chứng minh code nguyên vẹn: `flutter analyze` *No issues* + `flutter test`
  *All tests passed* ngay sau khi viết spec. Tool MCP không có trong phiên ⇒ khảo sát bằng cách đọc
  trực tiếp toàn bộ 10 file Dart/Kotlin (repo còn nhỏ) — thay thế tương đương về độ chính xác.

- [2026-09-21] Tạo **knowledge base `.project/`** (13 file: README, overview, architecture, state-routing,
  patterns, design-system, integrations, openspec, modules/{README + 4 module thực có}) + bộ file memory
  ở gốc: `context.md`, `operating_rules.md`, `CLAUDE.md` (mới), và **điền phần `PROJECT` trong `AGENTS.md`**
  (Role & Context / Navigation / 12 Critical Rules / Workflow / git convention — hết placeholder).
  - Ghi rõ trạng thái thật: **chưa có tính năng sản phẩm nào**, không auth/backend/payment; APK chưa
    từng build; repo **0 commit**. Nợ kỹ thuật K1–K10 nằm ở `.project/openspec.md` mục 3.
  - Đã kiểm: 60 file `.md` → **0 link nội bộ hỏng**; `.project/` **không** bị gitignore.
  - `openspec update` → *All 3 tool(s) up to date (v1.13.1)*, không drift; vẫn **0 change** đang mở.
  - Hạ tầng phiên: AgentMemory (`localhost:3111/health`) **không phản hồi** ⇒ không lưu memory/ADR được
    (không tự khởi động lại dịch vụ).

- [2026-09-21] **P0.5 (Project Bootstrap) — XONG PHẦN LÀM ĐƯỢC, 2/5 mục DoD đạt, 3 mục còn lại chưa kiểm được.**
  - **Precondition của P0.5 KHÔNG đạt** (P0 chưa có kết luận go/no-go; chưa biết A2DP hay HFP) → đã tự kiểm
    bằng bằng chứng và báo mâu thuẫn; **chủ dự án chọn "waive precondition"** ⇒ rủi ro đã ghi rõ trong báo cáo.
  - Đã làm: `flutter create` thật ở gốc repo (`com.aiassistant.phone`, minSdk 26, Android only); cấu trúc
    `lib/{core,audio,transcript,suggestion,trigger,ui,services}` + README từng thư mục; manifest 7 quyền;
    foreground service skeleton (`flutter_foreground_task` **11.0.3** — API khác hẳn bản prompt mô tả);
    `audio_session` 0.2.4 (`usage=media`, cố ý không `voiceCommunication`) + lắng nghe `becomingNoisy`;
    SQLite (sqflite) + secure storage cho API key; màn hình chính tối thiểu Sẵn sàng/Đang lắng nghe; lint 8 rule bổ sung.
  - Kiểm thử thật: `flutter analyze` → *No issues found*; `flutter test` → *All tests passed*;
    đọc trực tiếp source trong `~/.pub-cache` để đối chiếu API trước khi viết code.
  - **Chưa kiểm được** (nên 3 mục DoD chưa tick): app build/cài chạy thật, FGS chạy nền không crash,
    manifest build không lỗi — máy dev thiếu Android SDK, chưa có thiết bị. **Chưa commit** (repo vẫn 0 commit).
  - Báo cáo: **`.plan/P0_5-result.md`**. Chi tiết build/chạy: `README.md` gốc repo.
- [2026-09-21] **P0 (Audio Feasibility Spike) — CHƯA HOÀN THÀNH, dừng ở Precondition.**
  - Lý do: P0 yêu cầu 1 điện thoại Android thật + 1 tai nghe Bluetooth + USB debug; máy dev hiện
    `adb devices` rỗng, không có thiết bị nào. Toàn bộ 5 mục Definition of Done của P0 là đo đạc
    trên máy thật nên chưa mục nào được tính là đạt.
  - Đã làm phần không cần thiết bị: convert + quantize PhoWhisper base/tiny sang GGML q5_0,
    tải model Vosk tiếng Việt (small + bản lớn), viết tooling (`spikes/p0_audio/tools/`), đo baseline
    trên host bằng FLEURS có ground-truth, viết code app spike (Flutter + Kotlin + JNI whisper.cpp).
  - Phát hiện đáng chú ý: Vosk model nhỏ trả về RỖNG khi audio nhỏ tiếng (đúng tình huống mic để xa);
    quantize q5_0 gần như không mất chất lượng; PhoWhisper thắng Vosk rõ rệt (12-16% vs 40-53% WER).
  - Báo cáo theo định dạng mục 5 của AGENT_INSTRUCTIONS.md: **`.plan/P0-result.md`** (quy ước: mỗi phase 1 file `<PHASE>-result.md`).
  - Chi tiết + protocol test on-device: `spikes/p0_audio/README.md`.
  - Chờ người dùng cắm điện thoại; sau đó chạy Task 1-4 của `prompt_P0.md` rồi mới có kết luận go/no-go.
- [2026-09-21] Bổ sung bộ tài liệu điều phối ở gốc repo: `checklist.md` (đã/chưa/cần làm/cần hỏi),
  `features.md` (tính năng hiện tại & tương lai), `next.md` (roadmap + việc sắp tới), `faq.md`
  (thắc mắc/hiểu sai), `result_20260921-1517.txt` (kết quả phiên), `handoff_20260921-1517.md`
  (bàn giao phiên sau — viết thủ công vì skill `handoff` chỉ user gọi được), `LESSONS_LEARNED.md`
  (14 lỗi thật đã mắc + quy tắc chống tái phạm). Điền context dự án cho `openspec/config.yaml`
  và chạy `openspec update` (v1.13.1, không drift).
- [2026-09-21] Ghi chú hạ tầng: AgentMemory (`localhost:3111`) không phản hồi trong phiên này nên
  không lưu được memory; `context.md` / `operating_rules.md` chưa tồn tại (nên tạo ở P0.5 khi có
  khung app thật). OCR (`ocr` CLI) chưa được cài trên máy → bước review dùng cách đọc thủ công.
- [2026-09-22] **Verify P1E trên máy thật** qua adb (Pixel 3a / Android 12 / arm64, APK `9f4508e`):
  K27a–d **đạt** — transcript thật vào SQLite, crash recovery giữ đúng phiên cũ, phiên 8 ngày bị xoá cả
  dòng + mốc Push, và **migration v1→v2 chạy thật** (3 bảng + 2 index, dữ liệu `meta` cũ nguyên vẹn).
  DoD-4 (bấm nút Push) chưa kiểm — máy đang dùng, không tap bừa.
- [2026-09-22] **Phát hiện K29 (🔴) — ASR không đạt realtime** (1 chunk 4s ≈ 160s, RTF ≈ 39, 59 chunk bị
  bỏ): 4 nguyên nhân cộng dồn — `threads=2`/8 nhân; native build **variant Debug** (không set
  `CMAKE_BUILD_TYPE`, host P0 build Release); `GGML_NATIVE=OFF` không kèm `-march` (mất dotprod/fp16);
  chunk 4s bị pad ~30s bởi whisper. Thêm **K30** `abiFilters` vô hiệu (APK **155MB / 3 ABI**), **K31**
  `libjnidispatch.so` có trong APK ✅ (runtime chưa test), **K32** RAM chưa đo.
  Đã ghi bài học **A36–A40** + thứ tự sửa đề xuất vào `.plan/P1E-result.md`.
- [2026-09-22] **Sửa K29a+b+c + K30** (user duyệt) — commit `2ecdd3b`: `threads` mặc định = tự động
  `min(4, số nhân)` (+2 test khoá); `add_compile_options(-O3)` đặt trước `FetchContent_MakeAvailable`
  (cờ thư mục/target chèn sau cờ build type ⇒ mới thắng `-O0` của variant Debug);
  `-march=armv8.2-a+dotprod+fp16` cho arm64-v8a; `disable-abi-filtering=true` để plugin Flutter không
  ghi đè `abiFilters`. Thêm bước CI in `CMakeCache` + `compile_commands` + ABI thật trong APK.
  Verify local: `flutter analyze` sạch · **97/97 test**. CI `35680615247` **XANH** (6 phút, 1 ABI):
  log in `flags: -O3 -march=armv8.2-a+dotprod+fp16`, APK **115MB · 13 lib · ABI chỉ arm64-v8a**.
- [2026-09-22] **⚠️ Cờ `dotprod` crash thật trên máy** (A42): sau khi cài APK `2ecdd3b`, app chết
  `signal 4 (SIGILL, ILL_ILLOPC)` tại `libggml-cpu.so (ggml_vec_dot_q5_0_q8_0+256)` → `whisper_full`.
  `/proc/cpuinfo` của Pixel 3a: có `asimdhp` nhưng **KHÔNG có `asimddp`** ⇒ không có dot product.
  **Đã revert `-march` trong `bac1414`** (giữ `-O3`), ghi cảnh báo ⛔ + bằng chứng trong
  `CMakeLists.txt`, checklist K29c, bài học A42/A43. Đo được trên máy: `threads=4` ✓ (log AsrJni),
  model load ở loader thread ✓ (F1 giữ nguyên).
- [2026-09-22] **ĐO LẠI SAU FIX (bản `bac1414`) — nhanh hơn 35 lần:** latency 1 chunk 4s từ
  ~160 000 ms → **trung vị 4 511 ms** (RTF **39 → 1.13**); chunk bị bỏ từ 59 liên tục → **8 trong ~5
  phút**; **54 dòng transcript thật** trong 4.7 phút (khoảng cách trung vị 4 535 ms); **0 crash
  SIGILL**. ASR vẫn chạy (nên tắt bằng nút khi không đo). **Chưa đạt realtime** (RTF ≥ 1) ⇒ bước tiếp
  theo là **K33**: thử chunk 8–12s và `threads` 6 (chỉ đổi config, không đụng cờ build).
- [2026-09-22] **ĐO K33 XONG — ASR ĐẠT REALTIME trên máy:** A/B qua `AsrTuning` (bảng `meta`, không
  build lại): 4s/4 → RTF trung vị **1.47** · 8s/6 → **0.63** · **12s/6 → 0.31, 0 chunk bị bỏ**.
  Xác nhận nghi mel pad ~30s/chunk là thật (K29d). Máy test đang chạy config **12s/6**; mặc định code
  vẫn 4s/auto, chờ user chốt có đổi mặc định không. Crash recovery (25 dòng) tự xác nhận khi app
  restart giữa các lần đo. Chi tiết: `lib/audio/asr/README.md` mục 6b, `checklist.md` K33.
