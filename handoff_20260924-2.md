# HANDOFF — 2026-09-24 buổi 2 (Fix SnackBar không hiện do ScaffoldMessenger đặt sai vị trí)

## 1. Trạng thái git / CI / APK

- **Đã commit + push buổi này: `b4fa576`** (`fix(ui): SnackBar hien lai o moi tab — ScaffoldMessenger ra ngoai Scaffold`, 16 files, +534/−38) — CI sẽ tự build APK. Mốc nền trước đó: `b4904d3` (docs P5.4; CI run `35943885692` success).
- Commit thứ 2 (nhỏ, docs-only): bổ sung A73/A74 vào `LESSONS_LEARNED.md` sau khi phát hiện commit đầu ghi "A72-A73" nhưng A73 chưa tồn tại (bài học A73 thực tế hoá ngay tại chỗ) + cập nhật skill `flutter-dart-agent-pitfalls` (mục 6/7 + checklist 5→7 mục; `.agents/` bị gitignore, local-only).
- **Nhắc lại quy trình:** mọi lần push `main` đều **tự build lại APK** (`.github/workflows/build-debug-apk.yml`)
  ⇒ nên gộp nhiều thay đổi tài liệu vào cùng một lần push. **Không build APK ở máy dev** (không có Android SDK).
- **Local-only, KHÔNG commit** (dù không bị ignore): `TESTING.md`, `human.md`. Bị ignore: `.plan/`
  (gồm `prompt_fix_snackbar_messenger.md`, `fix_snackbar_messenger-result.md`, `manual_test_snackbar_messenger.md`),
  `.agents/`.

## 2. Bản đồ thay đổi chính (cho phiên sau)

| Vùng | File | Ghi chú |
|---|---|---|
| Cây widget | `lib/ui/root_scaffold.dart` | `ScaffoldMessenger(key: _messengerKey)` ra **NGOÀI**, `Scaffold` vào **TRONG**. `appBar`/`IndexedStack`/`BottomNavigationBar` không đổi nội dung |
| Seam test | `lib/ui/session_coordinator.dart`, `lib/ui/root_scaffold.dart` | Tham số tuỳ chọn `TestLlmService? testLlm` (mặc định `null` = `TestLlmService()`). **Logic `enqueueSnack`/`_drainSnackQueue` 0 dòng bị đổi** |
| Test mới | `test/snackbar_visibility_test.dart` | 4 test: Test LLM (OK/fail) + Bật lắng nghe từ tab Trang chủ + từ tab Lịch sử. Bất biến: SnackBar **không** có tổ tiên `IndexedStack` |
| Test cũ sửa | `test/p5_4_catchup_test.dart` | Hằng `_fixedNow` dùng chung cho fake DAO + service (sửa **time-bomb** — xem §5) |
| Tài liệu | `.project/modules/bootstrap-shell.md` | +cảnh báo mới về thứ tự `ScaffoldMessenger`; sửa schema cũ v4→v6 |

## 3. Quyết định đã chốt (không đổi lại nếu không có lệnh mới)

- **Thứ tự `ScaffoldMessenger` → `Scaffold` là RÀNG BUỘC** trong `root_scaffold.dart`. Đặt messenger vào
  `Scaffold.body` = tái tạo bug SnackBar vô hình. Đã ghi thành **cảnh báo** ở `bootstrap-shell.md` §7.
- **KHÔNG bỏ `Scaffold` riêng của `HistoryScreen`/`StatsScreen`.** Flutter đã tự xử: chỉ `Scaffold` gốc
  (`_isRoot` trong `ScaffoldMessengerState`) vẽ SnackBar ⇒ đúng 1 SnackBar.
- **KHÔNG sửa logic `enqueueSnack`/`_drainSnackQueue`** (lỗi ở cây widget, không ở hàng đợi).
- Seam `testLlm` là **chỉ để test**; app thật đi đường mặc định.

## 4. Việc còn lại (ưu tiên từ trên xuống)

1. **[DoD còn thiếu] Test tay trên máy thật** — theo `.plan/manual_test_snackbar_messenger.md` +
   **Việc 9 mới trong `human.md`** (bảng ghi kết quả đã có hàng 9). Đường nhanh: tải artifact
   `app-debug-apk` của run CI cho `b4fa576` ⇒ `adb install -r` ⇒ bấm **Test LLM** ở tab Cài đặt
   (ca chính), rồi **Bật lắng nghe** ở tab **Trang chủ** (ca phân biệt rõ nhất) và tab **Lịch sử**
   (kiểm không vẽ trùng 2 SnackBar).
2. Sau khi máy đạt: gộp vào buổi **K51** trong `checklist.md` (mục máy-thật đã ghi sẵn trong K51).
3. Không còn nợ time-bomb — cả 4 fake DAO đã tất định (A72).
4. ✅ Đã commit + push `b4fa576`; commit thứ 2 (docs-only) bổ sung A73/A74 + cập nhật skill.

## 5. Bẫy / bài học của buổi này

- **A72 (mới, đã xử DỨT ĐIỂM cả 4 file):** test double dùng **đồng hồ thật** trong khi SUT dùng
  **đồng hồ được bơm** ⇒ test có "hạn dùng", tự đỏ sau một mốc thời gian (xanh trước 03:00 ngày
  24/09/2026, đỏ sau đó). Đã sửa cả `p5_4_catchup_test.dart` lẫn 3 fake còn lại (`transcript_store`,
  `issue1_session`, `p5_1_history_retention`) — cutoff tính từ mốc ghi gần nhất trong state của fake
  (tất định), không còn `DateTime.now()` thật nào trong fake DAO.
- **A73 (mới, thực tế hoá ngay trong buổi):** commit đầu ghi "LESSONS A72-A73" nhưng A73 chưa tồn tại
  trong file — bắt bằng `grep -c` sau push, đã bổ sung. Quy tắc: commit message mô tả hiện trạng,
  không mô tả ý định.
- **A74 (mới):** thứ tự lồng widget đăng ký framework (`ScaffoldMessenger` phải ở **tổ tiên** của
  `Scaffold`) — bug im lặng, chỉ widget test bấm-nút-trong-cây-đầy-đủ mới bắt được.
- Khi test widget cần kiểm một SnackBar: **phải** bơm đồng hồ giả để xả timer (SnackBar 4s + hàng đợi
  4.1s/snack của coordinator) — xem A61 và helper `flushSnackTimers`.
- `ListView` của tab Cài đặt build lười ⇒ phải `scrollUntilVisible` trước khi bấm nút.

## 6. Cấm / không được làm (nhắc lại)

- **Không build APK ở máy dev.** Chỉ qua GitHub Actions.
- **Không** đổi thứ tự khởi tạo trong `main()`; **không** bỏ `try/catch` từng mảnh hạ tầng.
- **Không** tự commit code thuộc vùng loại trừ Ponytail khi chưa có xác nhận user. Buổi này **không** chạm
  DB/audio/quyền — nhưng vẫn **không tự commit** theo quy ước \"chờ user xác nhận\" của repo.
- **Không** tự cài/cấu hình OCR; nếu `ocr` vẫn không có ⇒ dùng fallback review mặc định.
- **Không** tắt service AgentMemory; nó đang **không phản hồi** (`localhost:3111/agentmemory/health`) —
  cần user tự khởi động nếu muốn dùng memory ở phiên sau.
- **Không** ghi secret/key vào file nào; khi in output git phải lọc `ghp_[A-Za-z0-9]+` (token nằm trong
  URL remote).
