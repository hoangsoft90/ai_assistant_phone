# handoff_20260921-1517.md — Bàn giao phiên cho agent/người tiếp theo

> Ghi chú: skill `handoff` có tồn tại trong hệ thống nhưng **agent không được phép tự gọi** (chỉ user gọi được), nên file này được viết thủ công theo cùng mục đích: đủ thông tin để phiên sau làm tiếp mà không cần đọc lại transcript phiên cũ.

## 1. Bối cảnh 30 giây

Dự án `ai_assistant_phone`: app Android cá nhân nghe hội thoại realtime và đưa gợi ý ngắn khi người dùng bấm nút (push-to-suggest). Ràng buộc cứng: 1 điện thoại + 4G + 1 tai nghe Bluetooth, **không server riêng**, chiều thu ASR **100% offline**, TTS **chỉ ra tai nghe**.

Đang ở **P0 — Audio Feasibility Spike** (`.plan/prompt_P0.md`). Toàn bộ kế hoạch: `.plan/production_roadmap.md` (15 phase), quyết định kiến trúc: `.plan/plan_final_v2.md`.

## 2. Trạng thái hiện tại

- **P0 CHƯA HOÀN THÀNH**: dừng đúng ở Precondition — không có điện thoại Android thật và tai nghe Bluetooth kết nối tới máy dev. 0/5 mục Definition of Done đạt.
- Đã xong phần **không cần thiết bị**: convert/model ASR, tooling, số liệu baseline trên host, code app spike (chưa build), báo cáo phase.
- Người dùng sẽ **cắm điện thoại vào chính máy dev này** sau khi code xong. Build APK sẽ làm **trên GitHub Actions** (người dùng sẽ đưa repo sau) — máy dev **không** cài Android SDK.

## 3. Bản đồ file quan trọng

| File | Nội dung |
|---|---|
| `.plan/P0-result.md` | Báo cáo phase P0 (định dạng mục 5 `AGENT_INSTRUCTIONS.md`) — đọc trước tiên |
| `.plan/prompt_P0.md` | Nhiệm vụ/DoD của P0 |
| `.plan/AGENT_INSTRUCTIONS.md` | Quy trình bắt buộc cho mọi phase (Precondition → Tasks → DoD → dừng chờ review) |
| `spikes/p0_audio/README.md` | Trạng thái chi tiết, số liệu, **protocol test on-device**, cách đẩy model |
| `spikes/p0_audio/app/README.md` | Kiến trúc app spike + cảnh báo không tái sử dụng `TtsTest` |
| `spikes/p0_audio/tools/` | 5 tool tái dùng cho P1C/P1D (convert, fetch audio test, normalize, đo WER whisper/vosk) |
| `spikes/p0_audio/reports/*.json` | Số liệu thô (bằng chứng) |
| `checklist.md`, `next.md`, `features.md`, `faq.md`, `LESSONS_LEARNED.md`, `working.md` | Tài liệu điều phối/ghi nhớ ở gốc repo |

## 4. Sự thật về code app spike (đừng hiểu nhầm là "đã chạy")

ĐỌC KỸ khi tiếp tục:
- **APK chưa từng được build**; **Kotlin/Gradle chưa từng được biên dịch** ở bất kỳ đâu.
- Đã test thật: `flutter analyze` (sạch), `flutter test` (pass 1 smoke test), `whisper_jni.cpp` biên dịch sạch bằng g++ host đối chiếu `whisper.h` thật, 3/3 symbol JNI khớp, hợp đồng MethodChannel/EventChannel Dart↔Kotlin khớp.
- Chưa test gì trên máy: mic, load model, transcript, route A2DP/SCO, TTS, foreground service 45–60 phút.
- Vì vậy lần build đầu tiên trên CI rất có thể sẽ lộ lỗi Gradle/NDK. Ba chỗ soi đầu tiên: cờ `GGML_*` trong `cpp/CMakeLists.txt`, `abiFilters` (đang chỉ `arm64-v8a`), `ndkVersion` trong `app/android/app/build.gradle.kts`.

## 5. Quyết định đã chốt trong phiên (kèm lý do)

1. **whisper.cpp pin theo commit `307869a...` + SHA256 tarball** trong `cpp/CMakeLists.txt` — để CI build đúng bản đã dùng convert model, tránh lỗi "load model thất bại" do lệch format GGML.
2. **Model không nhúng vào APK/assets**, đọc từ `/sdcard/Android/data/vn.p0spike.p0_spike/files/models/` — đổi model không phải build lại, không đẩy 133MB vào git/CI.
3. **Chỉ build ABI `arm64-v8a`** — giảm thời gian CI; điện thoại test là arm64.
4. **Chỉ định quantize q5_0** làm mặc định — đo được gần như không mất chất lượng so với f16 mà nhỏ 60–70%.
5. **Giữ mic ở `AudioSource.MIC`** (không `VOICE_COMMUNICATION`) — tránh kéo route Bluetooth sang SCO/HFP.
6. **Model nằm ngoài repo ở `/tmp/p0spike/`** (bản sao trong `spikes/p0_audio/models/`, đã gitignore) — vì `/home` chỉ còn ~396MB.
7. **Không commit gì** — chờ người dùng xác nhận (repo đang 0 commit).
8. **Không tạo OpenSpec change cho P0** — spike không phải thay đổi schema/API; sẽ tạo khi vào P0.5 (nhớ quy ước: tên change không bắt đầu bằng chữ số).

## 6. Việc tiếp theo — làm đúng thứ tự này

```bash
# 0) Xác nhận thiết bị đã cắm
adb devices -l

# 1) Build (chọn 1 trong 2)
#    a. Trên GitHub Actions (ưu tiên theo ý người dùng): cần repo + workflow Flutter/NDK 28.2
#    b. Tạm ở máy này: cài cmdline-tools + platform 36 + build-tools + NDK 28.2 vào /tmp
flutter build apk --debug            # debug trước, release sau

# 2) Cài + cấp quyền
adb install -r build/app/outputs/flutter-apk/app-debug.apk

# 3) Đẩy model vào thư mục riêng của app (đường dẫn app hiện trên UI)
DIR=/sdcard/Android/data/vn.p0spike.p0_spike/files/models
adb shell mkdir -p $DIR
adb push spikes/p0_audio/models/ggml-phowhisper-base-q5_0.bin $DIR/
adb push spikes/p0_audio/models/ggml-phowhisper-tiny-q5_0.bin $DIR/
adb push spikes/p0_audio/models/vosk-model-small-vn-0.4 $DIR/

# 4) Chạy test + thu log
adb logcat -c && adb logcat -s P0Spike:I P0SpikeJni:I | tee /tmp/p0_run.log
adb shell dumpsys audio | grep -iE "a2dp|sco|route"      # Task 2
adb shell dumpsys battery | grep -E "level|status"       # Task 1 (pin)
```

Sau đó: điền số liệu on-device vào `.plan/P0-result.md` (mục DoD) → viết kết luận go/no-go → cập nhật `checklist.md`/`next.md`/`working.md`.

## 7. Cần người dùng quyết (chưa chốt)

- Repo GitHub nào + có dựng workflow build APK luôn không?
- Có được xoá model f16/vosk-bản-lớn trong `/tmp/p0spike` (~700MB) để lấy chỗ build không?
- Báo cáo `.plan/P0-result.md` có cần copy sang thư mục được git commit không (`.plan` đang bị gitignore)?
- Có commit phần hiện tại không?
- Có cho phép tạo skill trong `~/.agents/skills/` từ `LESSONS_LEARNED.md` không?

## 8. Bẫy cần nhớ khi tiếp tục (đã trả giá trong phiên này)

Xem chi tiết ở `LESSONS_LEARNED.md`. Bốn cái dễ vấp nhất:
1. Đừng tin thông báo "Downloaded ✓" của tool tải file — phải **liệt kê file và kiểm tên bắt buộc** trước khi chạy bước sau (`hf download --include` đã bỏ sót `config.json`).
2. **Kiểm thử cái máy kiểm tra trước khi tin kết quả của nó** (2 lần script check của tôi báo sai: escape JNI thiếu quy tắc `.`→`_`; regex chỉ khớp 1 dạng call-site).
3. Khi một con số đo ra vô lý → **kiểm harness và mức âm lượng đầu vào trước khi kết luận về model** (Vosk "52%" thực chất là do audio nhỏ tiếng).
4. Báo cáo phase **phải theo đúng khuôn mẫu** của `AGENT_INSTRUCTIONS.md` mục 5, tên file `<PHASE>-result.md` — không tự sáng tạo định dạng/vị trí.

## 9. Thứ không được làm

- Không thêm cloud ASR cho chiều thu (mọi phase).
- Không copy cách gọi `TextToSpeech` trực tiếp của app spike sang P1F trở đi — phải qua `SafeTtsOutput`.
- Không tự commit/push khi chưa được yêu cầu; không tự cài/cấu hình OCR; không tự bật/tắt AgentMemory.
- Không nhảy sang P0.5 khi P0 chưa có kết luận go/no-go.
