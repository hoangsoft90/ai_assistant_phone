# LESSONS_LEARNED.md — bài học đã mắc & tự sửa (dự án ai_assistant_phone, P0 → P1F, phiên 2026-09-21/22)

Mục đích: ghi lại **lỗi thật đã mắc và tự sửa** trong phiên, kèm cách phát hiện và quy tắc để không tái phạm.
Không phải danh sách lý thuyết — mỗi dòng dưới đây đều đã xảy ra trong phiên này.

## A. Lỗi đã mắc & cách chống tái phạm

| # | Lỗi | Phát hiện bằng cách nào | Nguyên nhân gốc | Quy tắc chống tái phạm |
|---|---|---|---|---|
| A1 | Tin thông báo thành công của tool tải file: `hf download --include ...` in ra "Fetching 6 files ✓ Downloaded" nhưng **thiếu `config.json`** | Bước sau (script convert) chết với `FileNotFoundError: config.json` | Mặc định rằng tool tải đủ file mình liệt kê, không kiểm chứng lại | Sau **mọi** bước tải/fetch, phải liệt kê file thực có và assert các tên bắt buộc trước khi chạy bước phụ thuộc. Không tin dòng "Downloaded ✓" |
| A2 | Script tự viết kiểm symbol JNI sai → báo **MISS cả 3 hàm** (thực tế đúng) | Thấy kết quả vô lý (3/3 miss) nên soi lại script | Quy tắc mangle của JNI gồm **2 phép biến đổi** (`_`→`_1` **và** `.`→`_`), tôi chỉ làm phép đầu | Khi một checker báo "hỏng toàn bộ", **nghi checker trước tiên**. Viết checker phải tự kiểm trên 1 case đã biết là đúng |
| A3 | Script check hợp đồng MethodChannel dùng regex chỉ khớp dạng `invokeMethod<...>('x')`, bỏ sót call-site thật (`_call('x', ...)`) → báo sai "Kotlin có mà Dart không gọi" cho 4 method | Kết quả mâu thuẫn với việc đọc code, phải grep lại bằng mẫu khác | Viết regex theo *hình dung* về call-site thay vì theo code thật | Trước khi tin output của grep/script, chạy nó trên mẫu đã biết sẽ khớp; nếu 0 hoặc quá nhiều kết quả thì sửa mẫu rồi mới kết luận |
| A4 | Cùng một lệnh check còn lỗi `TypeError: set + list` ở lần chạy đầu | Lệnh trả exit code 1 | Trộn `list` và `set` trong biểu thức | Chạy từng phần nhỏ, đừng gộp 3 phép kiểm khác loại vào 1 script dài ngay lần đầu |
| A5 | Nộp "báo cáo" cho người dùng với cấu trúc tự nghĩ, **không theo khuôn mẫu** mục 5 của `AGENT_INSTRUCTIONS.md`, nhưng trình bày như thể đã theo | Người dùng hỏi lại: "Báo cáo theo đúng định dạng ở mục 5 chưa?" | Không đọc kỹ template trước khi viết; ưu tiên diễn đạt của mình | Khi tài liệu quy định khuôn mẫu đầu ra → xuất **đúng heading, đúng thứ tự**, rồi tự đối chiếu từng heading trước khi nói "xong" |
| A6 | Đặt báo cáo phase ở vị trí tự nghĩ ra (`spikes/p0_audio/README.md`) thay vì file `<PHASE>-result.md` cạnh các prompt | Người dùng yêu cầu: "ghi báo vào từng file md, vd: P0-result.md" | Không suy ra quy ước từ tên file có sẵn (`prompt_P0.md` → `P0-result.md`) | Khi tài liệu có quy ước đặt tên/vị trí (dù chỉ qua ví dụ), **suy ra và làm theo**, hoặc hỏi — không tự chọn chỗ khác |
| A7 | Để lại `test/widget_test.dart` mặc định của `flutter create` trỏ tới class `MyApp` đã bị tôi xoá → `flutter test` sẽ fail trên CI | Rà lại danh sách file sau khi thay `lib/main.dart` | Thay file sinh tự động mà không cập nhật các file phụ thuộc cũng do tool sinh | Sau khi thay `main.dart` sinh tự động, **ngay lập tức** sửa/xoá test sinh kèm; coi `flutter create` là "code phải bảo trì", không phải nền trơ |
| A8 | Viết 3 API không ai gọi (`shutdown()`, `WhisperEngine.loaded`, `TtsTest.close()`) rồi phải xoá | Tự soát dead code theo bước review | Thêm method "cho đủ bộ" theo thói quen | Với code throwaway/spike: chỉ viết cái có caller. Method vòng đời không có chỗ gọi thì không viết, và ghi rõ lý do nếu cố tình bỏ |
| A9 | Suýt báo WER của Vosk là **52%** như số cuối, trong khi 2/10 clip trả về rỗng | Kiểm mức âm lượng (RMS/peak) của input rồi thử chuẩn hoá gain | Đổ lỗi cho model trước khi kiểm dữ liệu vào | Khi số đo vô lý: (1) kiểm harness, (2) kiểm dữ liệu vào (level, sample rate, format), (3) mới kết luận về model. So sánh nhiều engine thì phải **chuẩn hoá input như nhau** |
| A10 | Gộp `flutter --version` (lần chạy đầu, tải ~117MB Dart SDK) vào cùng lệnh với các probe rẻ → lệnh timeout 30s, mất 1 lượt + 1 lần chạy lại | Lệnh trả "Command timed out after 30 seconds" | Không lường tool có "first-run cost" | Tách lệnh: probe rẻ (`which`, `ls`) riêng; lệnh chạy-lần-đầu (`flutter`, `npx`, `dart`, `gradle`) riêng với timeout dài |
| A11 | `code_search` quét cả thư mục `.plan/` → 56 match, chạm giới hạn output, tốn token vô ích | Phải đọc lại kết quả bằng cách khác | Không khoanh vùng tìm kiếm dù đã biết file cần | Khi đã biết file/thư mục đích → giới hạn `cwd` vào đúng chỗ và dùng pattern hẹp, hoặc đọc thẳng file |
| A12 | Copy 133MB model vào repo khi `/home` chỉ còn ~830MB, **chỉ cảnh báo hệ quả sau khi copy** (còn ~396MB) | Kiểm `df -h` sau khi copy | Chỉ kiểm dung lượng trước bước tải nặng, quên bước copy vào repo | Trước khi ghi một artifact lớn vào đâu: tính **headroom còn lại sau khi ghi** và cả nhu cầu của bước kế tiếp (build cache), báo trước chứ không báo sau |
| A13 | Lên kế hoạch review tự động dựa trên `git diff`/OCR/`detect_changes` trong repo **0 commit** — tất cả đều vô dụng | Chạy `git log`/`git status` mới thấy repo chưa có commit nào | Không kiểm trạng thái git trước khi thiết kế quy trình review | Kiểm `git log`/`git status` **sớm**; repo 0 commit ⇒ mọi tool dựa trên diff đều vô nghĩa, chuyển ngay sang review thủ công |
| A14 | Diễn đạt dễ gây hiểu nhầm là app đã chạy/đã kiểm thử, trong khi chưa từng build | Tự soát lại khi người dùng hỏi "viết xong đã test code chưa?" | Trình bày kết quả theo hướng tích cực, thiếu ma trận rõ ràng | Luôn nêu **ma trận test**: cái gì đã chạy (kèm lệnh + kết quả) vs cái gì **chưa từng** chạy (kèm lý do). Không dùng từ "đã test" cho phần chưa chạy |
| A15 | Viết YAML sai cú pháp: `- "Non-goals" phải nhắc lại...` (chuỗi có ngoặc kép rồi còn text phía sau) làm `openspec/config.yaml` không parse được | Chạy `yaml.safe_load` ngay sau khi sửa → báo lỗi dòng 33/34 | Quên rằng trong YAML, một scalar bắt đầu bằng ngoặc kép thì phải kết thúc luôn ở đó | Sau khi sửa file cấu trúc (YAML/JSON/TOML), **luôn parse lại ngay** bằng đúng parser mà tool dùng; đừng để đến lúc tool khác đọc mới phát hiện |
| A17 | Viết `$_config` trong string template Kotlin, mà property tên là `config` → **lỗi compile Kotlin** (`Unresolved reference '_config'`), chỉ lộ ở CI (máy dev không compile Kotlin) | CI run #2 trả về đúng 1 dòng `e:` | Gõ thừa `_` khi nhớ nhầm tên field có tiền tố | Sau khi sửa Kotlin: grep `\$_` (mọi string template tham chiếu biến) và đối chiếu với tên field thật trước khi push |
| A18 | Dùng `DOWNLOAD_EXTRACT_TIMESTAMP OFF` trong `FetchContent_Declare` → CI fail ở `configureCMakeDebug` với thông báo trông như **sai hash** (`URL_HASH is set to SHA256=…;DOWNLOAD_EXTRACT_TIMESTAMP;OFF but must be ALGO=value`) | Đọc log CMake nguyên dạng thay vì tin thông báo tóm tắt của AGP (`CXX1429 … Not searching for unused variables`) | cmake 3.22.1 (bản NDK đi kèm) **không hỗ trợ keyword này trong FetchContent_Declare** (chỉ từ 3.24) nên parser gộp keyword lạ vào giá trị `URL_HASH` | Viết CMake theo **đúng phiên bản cmake mà NDK cung cấp** (kiểm bằng cách đọc log: dòng `Install CMake x.y.z`), không theo bản cmake mới trên host. Khi CMake báo lỗi kiểu "tham số sai", đọc dòng `CMakeLists.txt:<số>` trong call stack — lỗi thật nằm ở đó, không phải ở tóm tắt của AGP |
| A19 | Tự bịa thêm bước "escape" cho symbol JNI: viết `Java_com_aiassistant_phone__asr_AsrNative_*` (hai `_` giữa `phone` và `asr`) | Người dùng rà lại diff và chỉ ra; đối chiếu lại quy tắc mangle JNI | Nghĩ rằng mọi mảnh tên package đều bị escape, trong khi quy tắc đúng: mỗi `.` → **một** `_`, chỉ `_` **nằm trong** identifier mới thành `_1` (spike P0 có package `p0_spike` → `p0_1spike`, chính chỗ này làm tôi nhớ nhầm) | Suy symbol JNI **từ tên package/class thật** theo từng bước (`. `→`_`; `_`→`_1`), không copy từ ngữ cảnh khác. Lỗi loại này **compile vẫn PASS**, chỉ lộ runtime (`UnsatisfiedLinkError`) — nên phải rà bằng tay trước khi tin CI |
| A20 | Định dùng package Flutter `vosk_flutter` như prompt gợi ý | Tra pub.dev API + đọc `android/build.gradle` của từng package trước khi cài | Chuỗi ràng buộc ẩn: `vosk_flutter` 0.3.48 yêu cầu Dart `<3.0.0`; `vosk_flutter_service` (bản mới, hợp Flutter 3.47) lại kéo `permission_handler: ^13` → `permission_handler_android` 14.x đòi **compileSdk 37** > AGP 9.1.0 max 36 (đúng lỗi đã làm CI fail 1 lần) | Trước khi thêm một plugin Flutter: kiểm **cả chuỗi** (Dart SDK constraint → pubspec deps của nó → compileSdk/minCompileSdk của module Android). Ưu tiên thư viện native dạng AAR/Maven chính chủ khi plugin Flutter có ràng buộc đụng phiên bản toolchain của app |
| A21 | Khai asset trong `pubspec.yaml` nhưng file không có trên máy dev (vì model 32MB không commit được) | Chạy `flutter test` → `No file or variants found for asset … Failed to build asset bundle` | Tưởng asset thiếu chỉ ảnh hưởng `flutter build apk`, thực tế **`flutter test` cũng fail** vì nó build asset bundle trước khi chạy test | Với file **không thể commit** (model/dump lớn): đừng khai trong `pubspec.yaml`; dùng Android asset (`android/app/src/main/assets/…` + `AssetManager`) hoặc tải ở bước build. Nếu đã khai thì bắt buộc phải có file ở máy dev |
| A22 | Tích hợp `vosk-android` (dùng JNA) — JNA tự giải nén `libjnidispatch.so` từ tài nguyên AAR | Tra chính sách AGP + case thật (LottieFiles/dotlottie-android#98) trước khi build | AGP ≥ 8 đóng native libs **không nén** (`useLegacyPackaging=false`) → JNA đi vào nhánh tìm `com/sun/jna/<platform>/libjnidispatch.so` trong classpath và có thể nổ `UnsatisfiedLinkError` — lỗi chỉ lộ **trên máy thật** | Với mọi thư viện dùng JNA: (1) `implementation("net.java.dev.jna:jna:<v>@aar")` (**biến thể `@aar`** mới có `.so`), (2) `packaging { jniLibs { useLegacyPackaging = true } }`, (3) thêm proguard keep rules cho `com.sun.jna.*`; ghi rõ đây là rủi ro cần xác nhận bằng logcat trên máy |
| A30 | Gọi helper cũ thiếu tham số: viết `invokeOnMain { result.success(null) }` trong khi hàm là `invokeOnMain(messenger, invoke)` → **8 lỗi compile Kotlin** (`No value passed for parameter 'messenger'`) chỉ lộ ở CI | CI run `35616645843` trả danh sách `e:` | Đoán chữ ký helper từ trí nhớ (cùng họ với A16/A19/A25) — và gốc sâu hơn: tham số `messenger` **không hề được dùng trong thân hàm** nên đáng lẽ đã bị bỏ từ đầu | Hai việc: (1) trước khi gọi helper đã có, `grep -n "fun <tên>" <file>` để đọc chữ ký thật; (2) tham số **không được dùng trong thân hàm thì xoá ngay** — tham số chết không chỉ gây nhiễu mà còn tạo ra đúng loại lỗi này |
| A31 | CI fail: Gradle wrapper tải `gradle-9.3.1-all.zip` từ services.gradle.org nhận **HTTP 504 Gateway Timeout** (redirect sang GitHub Releases) — commit được review chỉ sửa docs, không đụng code | CI run `35618380022`, log: `java.io.IOException: Server returned HTTP response code: 504` tại `org.gradle.wrapper.Download` | Coi mọi fail CI là lỗi code và đi tìm lỗi trong diff — không đọc kỹ stack trace để phân loại trước | Phân loại trước khi sửa: (1) **hạ tầng thoáng qua** (HTTP 5xx khi tải distribution/dependency) → retry là đủ, KHÔNG đụng code; (2) lỗi code → mới đọc diff. Phòng ngừa: cho bước tải distribution/dependency chạy lại 3 lần trong workflow (retry rẻ hơn build thật); đổi `-all` → `-bin` giảm 250MB tải mỗi lần; viết `-all` vào ghi chú AI CI là siêu dễ gây 504/hết đĩa cache |
| A26 | Đặt việc **nặng** (giải nén model 51MB + `Model()` + `Recognizer()`) ngay trong **method handler** của MethodChannel (P1D) | Review P1D đọc code Kotlin (máy dev không compile/chạy được Kotlin) | Tưởng handler chạy ở thread nền — thực tế handler chạy trên **main (platform) thread** | Trong handler của MethodChannel/receive-only channel: chỉ làm việc NHỆ. Việc nặng (I/O lớn, nạp model, tính toán lâu) phải đẩy sang executor riêng, và **`result.success/error` phải được gọi về main thread** (`Handler(Looper.getMainLooper()).post`). Nhớ kiểm cả các bridge CŨ (P1C cũng dính ở mức nhẹ hơn) |
| A27 | Worker có hàng đợi chỉ thoát dựa vào cờ `closed` (P1D) → `release()` bị kẹt (bỏ qua đóng native) rồi `load()` đặt lại `closed=false` ⇒ **thread cũ sống lại, dùng chung `recognizer` với thread mới** (native không thread-safe) | Review P1D | Nhớ rằng một cờ boolean là đủ để "khai tử" thread, bỏ qua khả năng cờ bị bật lại | Worker dài hạn phải có **"thế hệ" (generation token)**: `val myGen = ++generation` lúc start, vòng lặp thoát khi `generation != myGen`; và **worker tự giữ tham chiếu tới đối tượng native của mình** (`consume(rec, gen)`) thay vì đọc field dùng chung — như vậy thread cũ không thể chạm tài nguyên mới |
| A28 | Lời gọi `loadModel` không có timeout (P1D) → nếu native không bao giờ trả lời thì `init()` treo vĩnh viễn ⇒ **fallback của `AsrEngineSelector` không bao giờ chạy**, UI kẹt ở trạng thái bận mãi | Review P1D (đối chiếu lại lý do tồn tại của fallback) | Coi "native sẽ trả lời" là hiển nhiên, chỉ lo đường lỗi (throw) mà quên đường **treo** | Mọi lời gọi platform có thể treo (nạp model, ghi file lớn, gọi mạng) phải có **timeout**, và timeout phải **được coi là lỗi** để kích hoạt đường dự phòng. Đặt timeout trong `config` để test được (test bằng giá trị ms) |
| A29 | Streaming engine không **flush kết quả cuối** khi dừng (P1D: không gọi `getFinalResult()`) ⇒ mất đúng câu đang nói dở lúc tắt ASR; và Dart đóng transcript stream TRƯỚC khi gọi release nên text cuối cũng bị chặn | Review P1D | Nghĩ "dừng = đóng", quên rằng engine streaming còn giữ audio chưa chốt endpoint | Engine streaming PHẢI flush khi dừng (best-effort: chỉ flush khi worker đã dừng hẳn, không đụng native đang chạy). Phía Dart: gọi `releaseModel` (flush) **TRƯỚC** khi `close()` stream, và test bằng cách cho handler `releaseModel` phát ngược một `transcript` |
| A33 | `attach()` gọi `unawaited(detach())` rồi gán subscription mới ⇒ nhánh `detach()` treo ở `await` chạy tiếp **sau** đó và gán `_asrSub = null`, **đè lên subscription vừa gán** ⇒ stream cũ vẫn ghi dữ liệu nhưng không còn tham chiếu để huỷ (transcript bị ghi cả từ engine đã bị thay) | Test P1E `attach lần hai không nhân đôi dòng` fail (`Expected: <2> Actual: <3>`); đọc lại code thấy nhánh `await` chạy sau khi field đã bị đổi | Viết hàm huỷ theo thói quen "await cancel rồi null field", mà không để ý **thứ tự thời gian**: giữa `await` có một điểm nhường điều khiển cho code khác chạy | Mẫu bắt buộc cho mọi cặp start/stop giữ tài nguyên trong field: **lấy tham chiếu ra biến cục bộ + xoá field NGAY (đồng bộ, trước `await`) + huỷ trên biến cục bộ**. Với `attach` thì ngược lại: gán field mới trước, huỷ cái cũ sau bằng biến cục bộ. Luôn có 1 test "gọi start/attach lần hai không nhân đôi tác dụng" |
| A34 | `init()` cache future của lần khởi tạo bằng `_initFuture ??= _init()` ⇒ khi lần đầu **lỗi** (SQLite đang bị khoá, chưa có quyền...), future đã hỏng được cache **vĩnh viễn**: mọi lời gọi sau trả về đúng lỗi đó và store chết cho tới khi khởi động lại app | Tự rà khi review: thấy `_init()` ném lỗi ở nhánh DB hỏng nhưng `_initFuture` không được xoá | Nhầm giữa "chỉ chạy một lần" (idempotent) và "chỉ thử một lần" (không bao giờ thử lại) — hai tính chất khác nhau, nhưng code `??=` làm cả hai cùng lúc | Khi cache future cho việc idempotent: **reset cache khi lỗi** (`catch { _initFuture = null; rethrow; }`) để lần gọi sau thử lại được. Viết test "lỗi thoáng qua ⇒ lần sau thử lại thành công" |
| A35 | Sửa race theo mẫu capture-trước-khi-xoá xong thì `flutter analyze` báo `cancel_subscriptions` ("Uncancelled instance of 'StreamSubscription'") ở khai báo field — vì lint không lần theo được việc huỷ qua biến cục bộ | `flutter analyze` → 1 `info` | Lint chỉ nhận ra `cancel()` khi gọi trực tiếp trên field/expression mà nó theo dõi | Giữ nguyên code race-safe và thêm `// ignore: cancel_subscriptions` **kèm 1-2 dòng lý do** (huỷ ở đâu, vì sao phải qua biến cục bộ). Tuyệt đối không "sửa cho vừa lint" bằng cách cancel qua field — làm vậy là quay lại đúng cái race vừa sửa (A33) |
| A32 | Đổi icon launcher (chỉ resource Android + manifest) nhưng cứ cố verify bằng `flutter analyze` local; lệnh `flutter` bị tôi bọc `timeout 120/240` rồi bị SIGTERM **giữa lúc tải lại Dart SDK** → `bin/cache/dart-sdk` rỗng, các lệnh sau đó đều kèm "Failed to update packages" và mỗi lần chạy lại phải tải 233MB | `flutter analyze` không chạy được; thấy `bin/cache/dart-sdk` rỗng + `dart-sdk-linux-x64.zip` dở dang + `dart-sdk.old` rỗng | (1) Cắt ngang (`timeout`/SIGTERM) một lệnh `flutter` trong lúc nó đang refresh cache; (2) kỳ vọng `flutter analyze` verify được thay đổi **thuần Android resource** — local không có Android SDK thì analyze **không** kiểm được icon/XML, chỗ kiểm đúng là **build APK trên CI** | (1) Nếu `bin/cache/dart-sdk` rỗng và có `dart-sdk-*.zip` dở dang trong `bin/cache`: chạy `flutter --version` và **để cho xong** (resume bằng HTTP Range, ~4 phút ở 1.1MB/s) — đừng cap `timeout` ngắn; (2) thay đổi chỉ chạm `res/`/`AndroidManifest.xml` thì verify đúng cách = push cho CI build APK, chỉ chạy local analyze/test khi có đổi Dart |
| A36 | Đo trên máy thật thấy ASR chạy nhưng **1 chunk 4s mất ~160s** (RTF ≈ 39, host P0 là 0.29): tôi bắt đầu nghi model/quantize/thuật toán chia chunk, tức là đi tìm lỗi trong **code**, trong khi gốc nằm ở **cấu hình build** | CI log: `> Task :app:configureCMakeDebug[arm64-v8a]`; `grep -rn "CMAKE_BUILD_TYPE" android/` = **0 kết quả**; host P0 lại build `-DCMAKE_BUILD_TYPE=Release` (`spikes/p0_audio/tools/convert_phowhisper.sh:52`); `lib/ggml-cpu.so` 1.4MB đã strip DWARF (không đọc được cờ từ file) | So sánh tốc độ giữa **host build Release** và **máy thật build variant Debug** mà không kiểm cờ tối ưu của từng bên. Phụ: Dart đặt `threads = 2` mặc định trong khi máy 8 nhân ⇒ thêm 2–3x | Trước khi nghi thuật toán, so **3 thứ** giữa hai môi trường: (1) build type/cờ tối ưu, (2) số thread cấu hình so với số nhân thật (`nproc` / `availableProcessors`), (3) tập lệnh SIMD được bật (`-march`, dotprod/fp16). Muốn có bằng chứng cờ thì in `CMakeCache.txt` + `compile_commands.json` trong CI — **không đoán**, và đừng cố đọc cờ từ `.so` đã strip |
| A37 | APK debug **155MB** dù `ndk { abiFilters += listOf("arm64-v8a") }` đã ghi rõ; tôi vẫn tin là chỉ có 1 ABI cho tới khi giải mã central directory của APK đang cài | CI log có `configureCMakeDebug[armeabi-v7a]`, `[x86]`, `[x86_64]`; APK chứa `lib/{arm64-v8a,armeabi-v7a,x86_64}/libwhisper.so` + `libflutter.so` (3 bản mỗi lib) | Tin vào khai báo cấu hình thay vì **kiểm kết quả**: `abiFilters +=` trong Kotlin DSL không giới hạn được ABI ở setup này | Sau mỗi lần đổi cấu hình ABI: **đếm ABI thật trong APK** (cách làm ở A38) rồi mới được báo "đã giảm size". Muốn chắc thì `abiFilters.clear()` trước, hoặc dùng `splits.abi` |
| A38 | `adb pull` APK 155MB qua wifi **timeout >300s** ⇒ không soi được `.so`/asset bên trong APK đang cài | `Error: command timed out after 300 seconds` | Chỉ có một cách nghĩ ra là kéo cả file | Đọc **một file bên trong APK đã cài** không cần pull: (1) `pm path` lấy đường dẫn; (2) `adb exec-out "tail -c 700000 <apk>"` lấy EOCD + central directory; (3) parse ra `loff`/`csize`/`method` của entry; (4) `adb exec-out "tail -c +$((loff+1)) <apk> | head -c $csize"`; (5) **nếu `method=8` thì phải `zlib.decompressobj(-15)`** rồi mới phân tích ELF (làm sai bước này ⇒ parse ra rác, mất một vòng) |
| A39 | `uiautomator dump` để lấy toạ độ nút, nhưng dump toàn node của `com.android.systemui` (shade thông báo) — **điện thoại của user đang được dùng**, và nội dung thông báo cá nhân lọt vào ngữ cảnh | `package="com.android.systemui"` trong mọi node; `bounds` là các bảng thông báo Facebook/Shopee/Gmail | Dump trước, kiểm sau — không xác nhận cửa sổ nào đang focus | Trước mọi thao tác UI trên máy user: `adb shell dumpsys window | grep mCurrentFocus`; **không phải app** thì DỪNG, không tap, không screenshot, và **không ghi nội dung thông báo cá nhân vào tài liệu**. Tìm bằng chứng không cần UI: logcat, DB qua `run-as`, nội dung APK, `dumpsys meminfo` |
| A40 | Hai script Python nối VM Service của app đều fail: `InvalidURI` (script bị patch thành WS rỗng) rồi `HTTP 403` (scheme server từ chối) | `websockets.exceptions.InvalidURI: isn't a valid URI`; `InvalidStatus: server rejected WebSocket connection: HTTP 403` | Token của VM Service **đổi mỗi lần app khởi động**, mà tôi đã `adb logcat -c` trước `am start` (xoá mất dòng chứa URI), rồi patch script bằng regex khớp rỗng mà không kiểm lại giá trị | Lấy lại URI mỗi lần app khởi động: `http://127.0.0.1:<port>/<token>/` → đổi thành `ws://127.0.0.1:<port-forward>/<token>/ws`. **Không `logcat -c`** khi còn cần dòng URI. Sau khi patch file bằng regex, in giá trị ra kiểm — đừng giả định |
| A42 | Tôi thêm `-march=armv8.2-a+dotprod+fp16` cho arm64 để "lấy tốc độ SIMD" (đã được user duyệt trong kế hoạch) và app **crash ngay chunk ASR đầu tiên** trên máy thật | `dumpsys dropbox` + `dumpsys activity processes`: `signal 4 (SIGILL), code 1 (ILL_ILLOPC)` tại `libggml-cpu.so (ggml_vec_dot_q5_0_q8_0+256)` → `ggml_compute_forward_mul_mat` → `ggml_graph_compute` → `whisper_full`; `/proc/cpuinfo`: Features = `fp asimd evtstrm aes pmull sha1 sha2 crc32 atomics fphp asimdhp` (**KHÔNG có `asimddp`** = dot product), CPU part `0x803` (Cortex-A75) ×6 + `0x802` (Cortex-A55) ×2 | Giả định "CPU đời mới thì có dotprod", không kiểm `HWCAP` trước. Và cờ `-march` biên dịch cứng cho **cả file** ⇒ không có đường dispatch lúc chạy để cứu: chỉ 1 hàm dùng SDOT là cả app chết | Trước khi bật bất kỳ cờ kiến trúc nào: `adb shell grep Features /proc/cpuinfo` (thiếu `asimddp` ⇒ không có dotprod; `asimdhp` ⇒ có fp16), hoặc `getauxval(AT_HWCAP)` trong code. Muốn dùng dotprod cho máy hỗ trợ: **build 2 biến thể + chọn lúc chạy** (ggml `GGML_CPU_ALL_VARIANTS`/`GGML_BACKEND_DL`), KHÔNG dùng `-march` toàn cục. Khi lỗi chỉ lộ trên máy: đọc `dumpsys dropbox`/tombstone để lấy stack native, đừng đoán |
| A43 | Ba lần thao tác UI qua VM Service đều sai liên tiếp: dump rỗng (`No render tree root was added to the binding.`), rồi tap vào `(92,10)` = đỉnh màn hình (không trúng nút nào) | `_flutter.listViews` trả **2 view** (UI + engine của foreground service) ⇒ `getVM().isolates[0]` là engine KHÔNG có UI; và `offset=` trong `debugDumpRenderTree` là `parentData.offset` = **tương đối với node cha**, không phải toạ độ màn hình | (1) Giả định app chỉ có 1 isolate/engine; (2) đọc `offset=` như toạ độ tuyệt đối để tap | Với app nhiều engine: liệt kê `_flutter.listViews`, gọi `ext.flutter.debugDumpApp` từng isolate, chọn isolate có cây widget (>500 ký tự). Tap theo nhãn: parse cây → **cộng dồn offset theo tổ tiên** → tìm box kiểu nút → `input tap` với `dpr` (dump ghi `device pixel ratio`). Script đã viết lại và dùng được: `/tmp/vctl.py` (đọc chữ), `/tmp/vtap2.py` (tap có cộng dồn offset) |
| A44 | Phiên đo K33: 3 lần nối VM Service hỏng vì lỗi nhỏ của chính tôi — (1) `adb forward tcp:40721 42863` thiếu tiền tố `tcp:` ở đích ⇒ forward có trong list nhưng **kết nối chết im lặng** (curl 000); (2) cắt URI bằng `cut` dính cả token vào PORT ⇒ forward sai port; (3) token đã chứa `=` mà vẫn nối thêm `=` khi dựng path `/{token}=/ws` ⇒ **token đôi `==` → HTTP 403** | `forward --list` có dòng nhưng curl `000`; `adb: error: Invalid destination port: '43419/Iwo04aIMe7E='`; `InvalidStatus: HTTP 403` dù handshake HTTP 101 với path đúng | Vội, dựng URL bằng chuỗi nối tay thay vì in ra kiểm; quên rằng cú pháp adb forward đích **phải có** prefix `tcp:` | Quy tắc dựng kết nối VM Service: (1) forward luôn `adb forward tcp:L tcp:R`; (2) **in URI ra trước khi dùng** — path WS chuẩn là `/{token}/ws` với token CHỨA SẴN `=` (đừng thêm `=`); (3) sau khi forward, kiểm bằng WS handshake thật (HTTP 101) chứ đừng tin `curl /health` (nó không kiểm token, luôn 200) |
| A41 | Khai báo `ndk { abiFilters += listOf("arm64-v8a") }` trong `android/app/build.gradle.kts` mòng là đủ, nhưng APK vẫn đóng gói **3 ABI** (armeabi-v7a + arm64-v8a + x86_64 = đúng `PLATFORM_ABI_LIST`); tôi không tự sửa được vì **plugin Flutter ghi đè sau** | `FlutterPlugin.kt` > `configureAbiWithoutSplits()`: `extension.defaultConfig.ndk { abiFilters.clear(); abiFilters.addAll(PLATFORM_ABI_LIST) }` — chạy sau khi build.gradle của app khai báo; comment trong plugin nói "user settings take precedence" nhưng code thì `clear()` | Chỉ đọc cấu hình của **mình** mà không kiểm plugin nào khác ghi cùng property; đọc thêm mã plugin trong SDK (`/google/flutter/packages/flutter_tools/gradle/src/main/kotlin/`) khi cấu hình gradle không có tác dụng | Cách ly các lốối này: `disable-abi-filtering=true` trong `android/gradle.properties` là **điều kiện duy nhất** để plugin không ghi đè (`shouldProjectDisableAbiFiltering`). Quy tắc chung: khi một config gradle "không có tác dụng", tìm trong mã plugin/SDK xem có hook nào ghi cùng property sau đó — đừng sửa mù |
| A23 | Viết `AsrEngineSelector({required ConfigStore store}) : _store = store` → lint `prefer_initializing_formals` (2 info); mà sửa theo gợi ý `this._store` cũng **không được** vì **named parameter không được bắt đầu bằng `_`** | `flutter analyze` sau khi viết code mới | Chưa nắm làn ranh: field private thì **chỉ inject được qua positional param**, không qua named | Inject field private bằng **positional**: `AsrEngineSelector(this._store, [this._factory])` (hoặc để field public). Và nguyên tắc chung: sau mỗi lần thêm code Dart, chạy `flutter analyze` và sửa **hết** — kể cả `info` (`unnecessary_import`, `annotate_overrides` cũng cùng nhóm này) — đừng chỉ dừng ở việc hết `error` |
| A24 | Thêm getter `droppedTotal` (có thân hàm mặc định) vào abstract class `AsrEngine` → lớp test `_FakeEngine implements AsrEngine` **bị lỗi compile** `Missing concrete implementation of 'getter AsrEngine.droppedTotal'` | `flutter analyze` trong lần chạy kế tiếp | Nhầm rằng member có thân hàm mặc định thì lớp implement không phải viết lại — thực tế `implements` **bắt buộc khai lại MỌI member**, default body chỉ có tác dụng với `extends` | Khi thêm member vào interface (`abstract class` bị nhiều nơi `implements`): **grep tất cả `implements <Interface>`** và cập nhật hết trong cùng commit. Muốn thêm member "tuỳ chọn" cho lớp implement cũ mà không phải sửa chúng → phải là `extends`, hoặc tách thành interface mới |
| A25 | Viết test dùng hằng `PhoWhisperChannels.control` — **hằng này không tồn tại** (tên thật trong P1C là `AsrChannels.control`) → 2 lỗi `undefined_identifier` | `flutter analyze` | Suy tên hằng/class từ trí nhớ về phase trước thay vì đọc file nguồn | Trước khi dùng API/hằng của code **đã có sẵn**, grep tên thật trong file nguồn (vd `grep -n "class .*Channels" lib/audio/asr/*.dart`) rồi mới viết — tốn 1 lệnh, tránh cả vòng sửa–chạy lại |
| A45 | `SafeTtsBridge.playSynthesized` (P1F) ghi hết PCM vào `AudioTrack` rồi release **ngay** trong `finally` (`pause() → flush() → stop() → release()`) ⇒ toàn bộ tiếng TTS bị vứt đi, người dùng gần như không nghe được gì | Tôi tự review lại diff ngay sau khi push (user hỏi "review code những gì vừa làm") — **chưa kịp vấp ở sản phẩm**, nhưng nếu không review thì test case 1/3 của P1F (rút tai nghe GIỮA LÚC ĐANG ĐỌC) không thể chạy vì không có tiếng để nghe | Hiểu sai ngữ nghĩa `AudioTrack.write()` blocking: nó trả về khi dữ liệu đã **enqueue** vào buffer, KHÔNG phải khi đã phát ra loa ("will normally block until all the data has been enqueued for playback"); cộng thêm `flush()` vốn "discard audio data that hasn't been played back yet" | Phát PCM qua `AudioTrack` MODE_STREAM: sau vòng `write`, **chờ `playbackHeadPosition ≥ số frame đã ghi`** (timeout = độ dài clip + 2s cho khỏi treo) rồi mới `flush/stop/release`; kiểm cờ huỷ (generation) mỗi ~20ms để vẫn dừng tức thì khi mất thiết bị. KHÔNG `Thread.sleep(độ dài clip)` mù, và KHÔNG release ngay sau `write` |
| A46 | Push 2 commit P1F + để CI build APK (#22, #23) **rồi mới** review ra lỗi High (A45) ⇒ phải sửa + build lại lần 3 (#24); bản APK đã đưa user để cài (#23) thành lỗi thời | User hỏi "review code những gì vừa làm" → chạy review mới lộ | Đặt bước Code Review **sau** push/build. Vì repo chỉ build được qua CI (không Android SDK ở máy dev), mỗi vòng như vậy tốn ~7 phút CI **và** làm lệch bản APK đang được phát hành để test | Với dự án build-qua-CI: **chạy hết Code Review TRƯỚC khi push**. Nếu buộc phải push sớm (vd cần đóng nợ "Kotlin chưa từng compile" để biết code có build được): chỉ coi run đó là **run kiểm compile**, KHÔNG đưa APK cho user dùng thử, và nói rõ "bản cuối sẽ là run #N sau review" |
| A8 (**lần 2**) | Lại viết API "cho đủ bộ" không có caller trong module mới: `SafeTtsOutput.dispose()`, method kênh native `release`, `TtsDevice.id/address`, `stateListenable` | Tự soát dead code ở bước review: `grep -rn "stateListenable" lib/` → 0 kết quả ngoài khai báo | Bài học A8 đã ghi từ P0 nhưng ở dạng "tự soát" — không phải bước bắt buộc có lệnh cụ thể ⇒ không chặn được hành vi cũ | Biến nó thành **bước bắt buộc có lệnh**: trước khi commit, `grep -rn "<tên symbol>" lib/ test/` cho **từng symbol public mới thêm**, và xoá mọi symbol chỉ có 1 kết quả (chính khai báo của nó). Ghi danh sách đã grep vào báo cáo review |
| A16 | Refactor `CaptureChannelBridge` set → map `EngineChannels` nhưng quên đổi 2 dòng trong `dispatchChunk` (`holder.sink` thay vì `holder.pcmHolder.sink`) — **lỗi compile Kotlin** chỉ lộ khi `flutter build apk` | Người dùng rà lại và chỉ ra (máy dev không có Android SDK nên `flutter analyze`/`test` chỉ check Dart, không thấy lỗi Kotlin) | Sau khi refactor thành class bọc mới, cứ nhớ nhầm field ngắn cũ; không có vòng compile Kotlin ở máy dev để bắt | Sau **mỗi lần refactor Kotlin**, grep **tên field/ method cũ** trong toàn file trước khi kết thúc; nếu project không compile được Kotlin ngay (K11), coi mỗi lần sửa Kotlin là rủi ro cao và liệt kê tên field/method đã đổi vào báo cáo để người khác rà theo danh sách đó |
| A50 | Review P2: 3 chỗ trong tầng suggestion làm **`push()` NÉM ra UI** dù hợp đồng ghi "không bao giờ ném" — (1) `decoded['type'] as String?` trong parser; (2) `decoded['choices'] as List<Object?>?` + `choices.first as Map<String,dynamic>?` + `first?['message'] as ...` trong provider; (3) service chỉ bắt `on SuggestionException` | Tự viết test "phá" trước khi sửa: `type: 123` ⇒ `_TypeError: type 'int' is not a subtype of type 'String?'`; `{"choices":"x"}` ⇒ `_TypeError: type 'String' is not a subtype of type 'List<Object?>?'`; provider ném `TypeError` ⇒ xuyên thẳng qua service | Dùng `as` để "chắc chắn" kiểu của dữ liệu do BÊN NGOÀI sinh ra (phản hồi HTTP/LLM) — `as` ném `TypeError`, mà `TypeError` **không phải** `Exception` nghiệp vụ nên mọi `on MyException` ở tầng trên đều hụt | Với dữ liệu không do mình kiểm soát: đọc bằng `is` + gán `null` rồi xử lý (không dùng `as`); và ở tầng orchestrator luôn có một lưới cuối `catch (error)` chuẩn hoá lỗi lạ về lỗi nghiệp vụ trước khi trả cho UI. Test: ngoài case hợp lệ phải có case "envelope dị dạng" (chuỗi thay vì list, số thay vì chuỗi, object thay vì chuỗi) |
| A48 | P2: tầng trên (policy/service/UI) có nguy cơ gắn cứng vào Groq — đổi provider sau này phải sửa nhiều nơi, đúng thứ prompt P2 cảnh báo ("giá/model API có thể đổi") | Tự soát trước khi commit: grep tên provider/model trong tầng trên | Không có bước kiểm nào bắt buộc cho việc này ở các phase trước; dễ "tiện tay" khởi tạo provider ở nơi đang cần | Mọi phụ thuộc dịch vụ ngoài (LLM, storage, cloud) đi qua **interface + hằng số cấu hình** (`SuggestionConfig`); bản mặc định chỉ xuất hiện ở **một chỗ** (constructor service). Kiểm bằng `grep -rn "Groq\|api.groq" lib/suggestion/ --include="*.dart"` rồi loại trừ đúng file `groq_llm_provider.dart` |
| A49 | P2: prompt yêu cầu "JSON lỗi ⇒ retry 1 lần" **và** "timeout ⇒ NO_SUGGESTION sau ~3-4s" — nếu retry mọi lỗi thì một lần timeout 4s thành ~8s chờ, vi phạm yêu cầu thứ hai | Tự phát hiện lúc viết service (chưa có bug thật trên máy) | Retry là hành vi "mặc định an toàn" khi nghĩ về lỗi mạng, nhưng với app chờ tương tác realtime thì thời gian chờ là tài nguyên người dùng cảm nhận được | Exception mang cờ `retryable`: lỗi **nội dung** (parse/JSON sai) ⇒ retry; lỗi **vận hành** (timeout/mất mạng/HTTP) ⇒ fail ngay. Test phải **đếm số lần provider được gọi** (2 cho retry, 1 cho không-retry), không chỉ kiểm kết quả trả về |
| A47 | Điều khiển app Flutter qua adb để test: nhiều lần tap tưởng là "không vào app" (logcat trống tuyệt đối), dump UI tưởng rỗng, mất ~30 phút chẩn đoán sai hướng | Flutter semantics của `uiautomator` nằm ở **`content-desc`** (không phải `text`) nên parser tìm `text=` trả 0 kết quả; dump **lần 1 của mỗi lượt** thường rỗng (semantics chỉ bật khi có tool accessibility truy vấn); màn hình **doze** nuốt `input tap` im lặng; `AppLogger` (dart:developer) không hiện logcat nếu không có debugger | Ghép 3 lớp nhầm lần: parser sai attribute + dump 1 lần + doze ⇒ kết luận sai "app đông cứng" trong khi tap vào DB thật (probe sau này: `transcript_pushes` 2→3 dòng) | Quy tắc test-via-adb: (1) `input keyevent KEYCODE_WAKEUP` + `wm dismiss-keyguard` trước mọi thao tác; (2) dump **2 lần liên tiếp** và kiểm size trước khi parse; (3) đọc `content-desc`; (4) `input motionevent DOWN/UP` tin cậy hơn `tap`; (5) khi nghi "tap không vào", dùng **probe có phản ứng quan sát được** (đếm dòng DB, panel trạng thái) thay vì tin vào thiếu log |
| A51 | P3: ngưỡng "giữ 2 giây → Emergency" của nút nổi bị lệch **+100ms** khi tự đếm bằng `Timer` trong `GestureDetector.onTapDown` | Tự soát lại code ngay sau khi viết: `onTapDown` là callback của `PrimaryPointerGestureRecognizer` nên chỉ bắn sau `deadline` = `kPressTimeout` (100ms) hoặc khi thắng arena ⇒ đồng hồ thật bắt đầu muộn hơn lúc chạm; widget test sau đó đo được **đúng** mốc 2s sau khi đổi sang recognizer chuẩn | Tự viết cơ chế đếm thời gian cho một việc Flutter **đã có sẵn primitive** (`LongPressGestureRecognizer(duration:)`), và bỏ qua việc recognizer khác đã có `deadline` riêng | Ngưỡng thời gian của cử chỉ: dùng `RawGestureDetector` + `LongPressGestureRecognizer(duration: X)` (không dùng `GestureDetector.onLongPress` vì khoá cứng 500ms, không dùng `Timer` tự đếm). Hai recognizer trong arena tự giải quyết nên **không cần cờ "đã kích hoạt"** để chặn một cử chỉ chạy hai đường — bớt cả thủ công lẫn bug. Đo lại bằng `flutter test` (đồng hồ giả ⇒ đo được đúng mốc) thay vì chờ máy thật |
| A52 | P3: `dispose()` `TextEditingController` ngay sau `await showDialog(...)` ⇒ `TextField` còn đang trong cây (dialog mới bắt đầu animation thoát) đọc controller đã huỷ — lỗi kinh điển "A TextEditingController was used after being disposed" | Tự soát diff khi viết hộp thoại nhập API key (chưa xảy ra trên máy) | Nghĩ rằng `await showDialog` trả về **sau khi** route đã rời cây; thực tế route chỉ rời cây sau khi animation (200ms) kết thúc | Với dialog ngắn: **không tạo controller** — dùng `onChanged` + biến cục bộ (`var typed = ''`) để không có gì phải dispose. Nếu buộc phải có controller: dispose sau khi route hoàn tất (`await` thêm animation) hoặc để `State.dispose()` quản lý |
| A53 | P3: Kotlin đọc `call.argument<Double>("rate")` cho tham số tốc độ đọc — nếu phía Dart gửi số **nguyên** (`1` thay vì `1.0`) thì `ClassCastException` ⇒ `result.error` ⇒ Dart coi là lỗi kênh ⇒ **không đọc được gì cả** (mất cả tính năng chính vì một tham số phụ) | Tự soát lúc thêm tham số `rate` vào `speak` (đối chiếu `StandardMessageCodec`: int và double là 2 kiểu khác nhau) | Ép kiểu **cụ thể** khi đọc tham số kênh platform thay vì đọc theo họ kiểu (`Number`), và để một tham số **chất lượng** có quyền làm hỏng đường **chức năng** | Tham số tuỳ chọn qua platform channel: đọc bằng `(call.argument<Any>("x") as? Number)?.toDouble()`, thiếu/sai kiểu ⇒ bỏ qua và dùng giá trị mặc định, **không** trả `error`. Nguyên tắc chung: một tham số phụ (tốc độ, kích thước, trang trí) không bao giờ được phép làm hỏng đường chính; nếu nó vốn đã không đáng tin thì kẹp (clamp) thay vì từ chối |
| A54 | P4: `SafeTtsBridge` (native) giữ **MỘT field** `tempWav` cho file WAV của lần phát hiện tại, trong khi file được đặt tên theo *thế hệ* (`tts_<gen>.wav`) và mọi lần phát chạy trên cùng một thread ⇒ khi `speak()` mới tới, `generation++` làm vòng lặp của lần phát CŨ thoát ngay, rồi `finally { cleanTemp() }` của lần cũ xoá `tempWav` — tức **file của câu MỚI** ⇒ vài chục ms sau lần phát mới đọc `tempWav == null` và **không phát gì** (`không có file WAV để phát`). Vì TTS tổng hợp lâu hơn thời gian thread cũ thoát ra, đây gần như **tất định**, không phải race hiếm | Tự soát khi ráp pipeline P4 (chưa vấp trên máy): câu hỏi "đường Emergency phát ngay trong lúc đang đọc nudge thì native xử lý thế nào?" dẫn tới việc đọc lại `speak()`/`stopPlaybackInternal()`/`cleanTemp()` của P1F | Dùng **một biến dùng chung** cho tài nguyên **có nhiều thế hệ** (mỗi lần phát là một thế hệ), cộng thêm cleanup chạy **bất đồng bộ** trên thread khác ⇒ lần chạy cũ xoá tài nguyên không phải của nó. Đây là cùng họ với A33/K23 (generation token) nhưng ngược chiều: token đã có mà **vẫn còn một chỗ dùng chung chưa theo token** | Tài nguyên gắn với một "lần chạy" phải được **giữ cục bộ trong chính lần chạy đó** và cleanup xoá **đúng đối tượng mình sở hữu** (chỉ null field nếu field vẫn đang trỏ vào nó). Dấu hiệu để nghi ngờ ngay khi review: thấy `finally { clearShared() }` ở chỗ có thể đã có lần chạy mới ghi vào cùng field đó. Ngay cả khi đã có generation token, phải rà lại **mọi** state dùng chung mà thế hệ cũ còn chạm tới. **Đã áp ở P4** (user yêu cầu sửa luôn): `wavFor(gen)` là **nguồn duy nhất** cho đường dẫn/tên file; `playSynthesized` giữ `val wav = wavFor(gen)` của chính nó rồi `cleanTemp(wav)`; `cleanTemp(file)` **chỉ null field nếu `tempWav === file`**. Việc rà "mọi chỗ" đúng là đã lộ ra **call-site thứ hai cùng họ lỗi**: `onError(utteranceId)` của thế hệ CŨ cũng gọi cleanup chung ⇒ phải suy thế hệ từ `utteranceId` (`cleanTempOfGeneration`) — chứng minh rằng bước rà thêm này không phải thủ tục |

## B. Điều đã làm đúng, nên lặp lại

1. **Kiểm chứng model bằng dữ liệu thật có ground-truth** (10 clip FLEURS tiếng Việt) thay vì tin vào "file đã tạo thành công" — nhờ đó phát hiện được cả lỗi convert lẫn lỗi model.
2. **Biên dịch kiểm tra phần native trên host trước khi có thiết bị** (`g++` + `jni.h` của JDK + stub `android/log.h`) — rẻ, chặn được lỗi API `whisper.h` sớm vài ngày.
3. **Đối chiếu hợp đồng giữa các tầng bằng script** (symbol JNI, tên method/arg của MethodChannel) — bắt được đúng loại lỗi chỉ xuất hiện lúc chạy thật.
4. **Ghi lại sai khác với prompt gốc ngay lúc phát hiện** (`hf download` thiếu `config.json`, FLEURS trả WAV float32, version Vosk khác mô tả) — người sau không phải phát hiện lại.
5. **Dừng đúng lúc khi Precondition không đạt** thay vì "chạy tạm" rồi báo số liệu giả.
6. **Kiểm chứng lại một kết luận khó chịu** (Vosk có thật sự kém?) bằng cách thử thêm bản model lớn + chuẩn hoá âm lượng, tránh kết luận dựa trên 1 lần đo.
7. **Kiểm migration SQL bằng SQLite thật ngay trên máy dev (P1E):** máy không có Android SDK nên không thể chạy app, nhưng `sqlite3` của Python thì có — script **trích thẳng 5 câu DDL từ `app_database.dart`** (không chép tay), dựng DB v1 giả, chạy migration, rồi kiểm bảng/index/dữ liệu cũ + các truy vấn nóng (`segmentsSince`, `MAX`, xoá theo phiên). Bắt được lỗi schema trước khi tốn 1 vòng CI + 1 vòng cài máy.
8. **Để test viết ra kết luận, không phải để test xanh:** 3 test đầu của P1E fail — 2 cái là kỳ vọng sai của tôi, nhưng **1 cái lộ bug thật trong code production** (A33). Sửa theo test là đúng, sửa test theo code mới là sai.

## C. Đóng gói thành skill (chưa làm — cần người dùng cho phép)

- Nơi đặt skill: `~/.agents/skills/<tên-kỹ-năng>/SKILL.md` (hiện có 157 skill; repo cũng có `.agents/skills/` với 6 skill `openspec-*`). Ghi ra ngoài repo cần sự cho phép của người dùng.
- Theo skill `writing-skills`: **Iron Law — không viết skill khi chưa có test thất bại trước đó**. Nghĩa là phải chạy baseline (một agent không có skill này có tái phạm A1/A2/A9 không), ghi lại lời biện minh thực tế, rồi mới viết skill tối thiểu để bịt đúng những lỗi đó; sau đó chạy lại để xác nhận.
- Phạm vi dự kiến của skill: **kỷ luật kiểm chứng khi làm việc không thể build/chạy tại chỗ** — kiểm artifact đầu vào (A1), kiểm chính cái checker (A2/A3/A4), kiểm harness + dữ liệu vào trước khi kết luận số liệu (A9), nêu ma trận đã-test/chưa-test (A14), theo đúng khuôn mẫu tài liệu của dự án (A5/A6).
- Nhóm A10–A13 (chi phí tool, dung lượng đĩa, trạng thái git) nên nằm trong cùng skill ở phần "chuẩn bị môi trường" hoặc chuyển vào `operating_rules.md` của repo.
