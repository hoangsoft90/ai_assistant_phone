# HANDOFF — 2026-09-24 buổi 1 (P5.4: timeout theo use-case + phân tích bù phiên thiếu báo cáo)

## 1. Trạng thái git / CI / APK
- **`main` đi trước `origin/main` 4 commit P5.4** (sau khi push):
  `62a959c` fix(llm) tách timeout → `f0d50ca` feat(coach) catch-up → `a36ce8b` test → `docs:` đồng bộ.
  Mốc nền trước đó: **`d4f4943`** (docs sau issue1_fix + follow-up; `main == origin/main` tại thời điểm bắt đầu).
- **Push để CI build APK mới** (đúng quy trình: chỉ build qua GitHub Actions, không build ở máy dev).
- **Local-only, KHÔNG commit** (dù không bị ignore): `TESTING.md`, `human.md` — theo thông lệ giữ ngoài commit.
  Bị ignore nên chắc chắn không vào git: `.plan/` (gồm `prompt_P5_4.md`, `P5_4-result.md`), `.agents/`
  (gồm skill `flutter-dart-agent-pitfalls`).

## 2. Bản đồ thay đổi chính (cho phiên sau)
| Vùng | File | Ghi chú |
|---|---|---|
| DB v6 | `lib/core/constants.dart` (`databaseVersion = 6`), `lib/services/storage/app_database.dart` | Nhánh `oldVersion < 6` mới + helper dùng chung `onCreate`; **KHÔNG sửa** nhánh `< 2`..`< 5` |
| Timeout | `lib/core/constants.dart` (`postReviewTimeout` 5′ · `socketConnectTimeout` 10s · `llmTimeout` **giữ 4s**), `lib/suggestion/llm_http_client.dart` (MỚI), `groq_llm_provider.dart`, `test_llm_service.dart` (30s), `session_summary.dart` | Hai lớp timeout: nối (10s) vs chờ phản hồi (4s/5′/30s) |
| Catch-up | `lib/coaching/pending_analysis_service.dart` (MỚI), `lib/services/storage/transcript_dao.dart` (`SessionText` + `joinSessionText` + 3 method), `lib/transcript/transcript_store.dart` (uỷ quyền DAO), `lib/coaching/post_review_service.dart` (`runForSession`/`_analyze`/`infrastructureFailure`), `lib/ui/session_coordinator.dart`, `lib/ui/history_screen.dart` | Tuần tự, throttle 6h, ghi mốc thử TRƯỚC khi gọi LLM, dừng lượt khi lỗi hạ tầng |
| Test | `test/p5_4_timeouts_test.dart` (MỚI, 9), `test/p5_4_catchup_test.dart` (MỚI, 16), `test/p5_2_session_title_test.dart`, `test/{transcript_store,issue1_session,p5_1_history_retention}_test.dart` | p5_2 đổi assert version sang **quy tắc** (`== max(oldVersion < N)`) |

## 3. Quyết định đã chốt (không đổi lại nếu không có lệnh mới)
- **Push giữ 4s** — ràng buộc UX cứng; đường cứu là Offline Nudge Cache (P3), KHÔNG nới timeout.
- Post-Review + tóm tắt phiên dùng chung mốc **5 phút**; Test LLM **30s**.
- Catch-up **tuần tự, không song song**, tối đa **5 phiên/lượt**, **throttle 6h/phiên**, **dừng cả lượt
  khi lỗi hạ tầng** (phiên khác cũng sẽ lỗi y hệt), tiếp tục khi lỗi nội dung.
- **Không** thêm Android background service/WorkManager/permission; "ngầm" = bất đồng bộ trong app đang mở.
- Logic ghép/cắt transcript **một bản duy nhất** (`joinSessionText`) — mọi đường phải đi qua nó.
- Migration chỉ **thêm nhánh mới**, nhánh cũ nguyên vẹn từng chữ.

## 4. Việc kế tiếp
1. **Chờ CI build APK xong** (sau push) → tải `app-debug-apk` → cài máy thật.
2. **Buổi test máy thật gộp K51** (giáo trình ở `human.md`), riêng phase này cần:
   (a) migration **v5→v6** trên DB có sẵn — transcript + báo cáo cũ phải còn nguyên;
   (b) Post-Review với endpoint **thật chậm (>4s)** ⇒ có báo cáo (không "hết thời gian chờ");
   (c) **tắt mạng lúc "Kết thúc buổi"** ⇒ mở lại app / bấm nút Lịch sử ⇒ buổi đó có báo cáo;
       đồng thời xác nhận catch-up **không** làm chậm việc mở app.
3. Sau K51: cập nhật K48/K49/K50/K51 trong `.project/openspec.md` + `next.md`.

## 5. Bẫy cần nhớ (chi tiết ở LESSONS_LEARNED.md A67–A71)
- **A67**: sau `await` trong `try/catch`, biến local gán trong `try` **không** được promote; đừng "sửa"
  bằng `!` — tạo `final X? r = biến;` rồi rẽ nhánh trên `r`.
- **A68**: thêm member vào interface dùng chung ⇒ chạy `flutter analyze` ngay; đừng kết luận qua
  `grep -c noSuchMethod <file>` (đếm theo **file**, không theo **class**).
- **A69**: assertion liên quan version/số lượng phải kiểm **bất biến**, không ghim số của thời điểm viết.
- **A70**: `pumpAndSettle` + animation vô hạn (`CircularProgressIndicator`) sẽ treo test.
- **A71**: cẩn thận ngoặc lồng trong `unawaited(() async … ()())`.
- Xem thêm A60–A66 (HttpOverrides, TestWidgetsFlutterBinding chặn HTTP, fake DAO implements interface).

## 6. Thứ KHÔNG được làm
- Không nới timeout của Push khỏi 4s nếu không có lệnh mới.
- Không sửa/xoá nhánh migration cũ; không xoá/tạo lại DB người dùng.
- Không chạy catch-up song song hoặc khi chưa có API key; không chặn UI.
- Không dùng `voiceCommunication` audio usage; không build APK tại máy dev (không SDK, `/home` gần đầy).
- Không tự commit vùng audio/DB khi thiếu xác nhận user.
