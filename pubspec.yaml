name: ai_assistant_phone
description: "Trợ lý AI hỗ trợ giao tiếp realtime trên Android (cá nhân)."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.13.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # --- Dependencies của khung P0.5 (danh sách cố ý giữ tối thiểu) ---

  # Foreground service (task 4): giữ tiến trình sống khi màn hình tắt / app bị minimize.
  flutter_foreground_task: ^11.0.3

  # Audio session (task 5): cấu hình thuộc tính audio phía hệ thống.
  # Lưu ý: package này cấu hình attributes cho PHÁT (playback). Việc chọn nguồn thu
  # (mic điện thoại) thuộc P1A và KHÔNG đi qua package này.
  audio_session: ^0.2.4

  # Lưu trữ (task 6): chọn SQLite cho transcript store của P1E.
  sqflite: ^2.4.4
  path: ^1.9.1

  # Lưu API key (dùng từ P2) — keystore/keychain của hệ điều hành.
  flutter_secure_storage: ^11.2.0

  # P2: gọi Groq chat completions (HTTP client chuẩn, timeout config được).
  http: ^1.5.0

  # Quyền runtime (mic + notification). Cần thật để foreground service type=microphone
  # chạy được trên Android 14+ và để notification hiện trên Android 13+.
  # PIN 12.0.1 (không nâng lên 13/14): permission_handler_android 14.x đòi compileSdk 37, mà
  # Flutter 3.47.2 + AGP 9.1.0 chỉ hỗ trợ tối đa compileSdk 36 → CI fail ở checkDebugAarMetadata
  # (run #1: https://github.com/hoangsoft90/ai_assistant_phone/actions/runs/35600799560).
  # Bản 12.x dùng permission_handler_android 13.x (compileSdk 35, Flutter >=3.24) — đủ cho nhu cầu
  # runtime-permissions của app (RECORD_AUDIO + POST_NOTIFICATIONS). Xem lại khi nâng SDK/AGP.
  permission_handler: 12.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  fake_async: ^1.3.3

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # P1C: model PhoWhisper-tiny q5_0 (29MB, convert từ P0) đóng gói trong APK.
  # Đổi khác so với P0: spike đọc model từ thư mục ngoài + adb push để không phải build lại;
  # app thật dùng asset để cài là chạy (không cần adb). Model base q5_0 (53MB) KHÔNG đóng gói
  # — chỉ tiny (ràng buộc kích thước APK; base để đo lại sau nếu tiny kém quá).
  assets:
    - assets/models/ggml-phowhisper-tiny-q5_0.bin

    # P1D — KHÔNG khai báo model Vosk ở đây (khác model PhoWhisper ở trên), CÓ CHỦ Ý:
    # - Model Vosk (zip 32MB) không commit vào git (máy dev chỉ còn ~150MB đĩa) nên file không có
    #   ở máy dev; mà asset khai báo trong pubspec MÀ THIẾU FILE thì `flutter test` fail ngay
    #   ("Failed to build asset bundle") — đã thử thực tế.
    # - Vosk đọc model qua Kotlin bằng AssetManager (tự giải nén từ zip), Dart không cần đọc bytes
    #   của asset này, nên dùng Android asset là đủ: kotlin đọc `models/vosk-model-small-vn-0.4.zip`.
    # - CI tải model vào android/app/src/main/assets/models/ (có kiểm SHA256) trước khi build;
    #   xem .github/workflows/build-debug-apk.yml và lib/audio/asr/README.md.

  

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
